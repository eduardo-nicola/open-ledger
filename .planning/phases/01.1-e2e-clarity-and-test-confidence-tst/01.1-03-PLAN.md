---
phase: 01.1-e2e-clarity-and-test-confidence-tst
plan: 3
type: execute
wave: 3
depends_on:
  - "01.1-02"
files_modified:
  - tests/e2e/auth.setup.ts
autonomous: true
requirements:
  - TST-06

must_haves:
  truths:
    - "Erros do setup citam variável ou ação única de correção quando possível"
    - "Mensagem de timeout em modo google não sugere 2FA como causa principal quando credenciais de teste estão ausentes"
  artifacts:
    - path: "tests/e2e/auth.setup.ts"
      provides: "Mensagens acionáveis"
      contains: "[E2E setup]"
---

<objective>
Melhorar mensagens de erro do setup Playwright (TST-06) para que falhas comuns (env ausente, seed, modo auth errado) apontem **uma** correção clara, sem mudar a lógica de autenticação.

Purpose: Reduzir tempo de diagnóstico quando smoke falha localmente ou em CI.
Output: `auth.setup.ts` com prefixo consistente e ramos de erro mais específicos.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/e2e/auth.setup.ts
@tests/e2e/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prefixo e mensagens acionáveis em auth.setup.ts</name>
  <files>tests/e2e/auth.setup.ts</files>
  <read_first>
    - tests/e2e/auth.setup.ts (implementação completa)
    - tests/e2e/README.md (variáveis documentadas)
  </read_first>
  <action>
1. Definir constante no topo do arquivo (após imports): `const E2E_SETUP_PREFIX = '[E2E setup]'` (string exatamente assim).

2. Refatorar todos os `throw new Error(` do arquivo para que a mensagem comece com ``${E2E_SETUP_PREFIX} `` (espaço após o sufixo).

3. Quando `!supabaseUrl || !anonKey`, substituir a mensagem por texto que inclua **literalmente** as substrings: `.env.local`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, e `tests/e2e/README.md`.

4. No ramo `persistSessionWithPassword`, manter a dica de seed mas prefixar; garantir que a mensagem inclua **literalmente** `supabase db reset` e `test@open-ledger.local`.

5. Em `waitForProfileRoute`, quando estourar timeout:
   - Se `process.env.E2E_AUTH_MODE === 'google'` **e** (`!process.env.E2E_GOOGLE_TEST_EMAIL` **ou** `!process.env.E2E_GOOGLE_TEST_PASSWORD`), lançar erro cujo texto mencione **literalmente** `E2E_GOOGLE_TEST_EMAIL` e `E2E_GOOGLE_TEST_PASSWORD` e explique login manual até `/profile`.
   - Caso contrário (credenciais presentes ou modo não-google), manter timeout com mensagem que inclua `E2E_MANUAL_AUTH_TIMEOUT_MS` mas **não** liste "2FA" como primeira causa; se mencionar 2FA, restringir a um trecho entre parênteses após a causa principal de timeout.

6. Não alterar fluxo de controle (ordem de chamadas, condições de modo) além do necessário para o item 5.
  </action>
  <verify>
    <automated>grep -q "const E2E_SETUP_PREFIX = '[E2E setup]'" tests/e2e/auth.setup.ts && grep -q '[E2E setup]' tests/e2e/auth.setup.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/auth.setup.ts` contém a linha exata `const E2E_SETUP_PREFIX = '[E2E setup]'`
    - Todo `throw new Error` no arquivo inclui a substring `[E2E setup]` (grep -c throw e grep -c E2E setup: contagem de throw deve ser igual à contagem de linhas throw que contêm o prefixo — verificar com leitura ou grep)
    - Pelo menos um `throw` contém `supabase db reset`
    - Pelo menos um `throw` contém `tests/e2e/README.md`
  </acceptance_criteria>
  <done>Mensagens TST-06 implementadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mensagens stderr/log | Não ecoar valores de env; apenas nomes de variáveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-04 | Information Disclosure | Mensagens de erro | mitigate | Nunca incluir `E2E_GOOGLE_TEST_PASSWORD` ou tokens; apenas nomes de variáveis |
</threat_model>

<verification>
```bash
grep -q "E2E_SETUP_PREFIX" tests/e2e/auth.setup.ts
npm run test:e2e:smoke
```
</verification>
