---
phase: 01.1-e2e-clarity-and-test-confidence-tst
plan: 4
type: execute
wave: 4
depends_on:
  - "01.1-03"
files_modified:
  - tests/e2e/README.md
autonomous: true
requirements:
  - TST-07

must_haves:
  truths:
    - "README define checklist mínima de PR para E2E (smoke password + expectativa de Google)"
    - "README declara que não há workflow GitHub Actions E2E no repositório até segunda ordem, ou aponta para o arquivo se for criado"
  artifacts:
    - path: "tests/e2e/README.md"
      provides: "Portão de PR"
      contains: "## Portão de PR"
---

<objective>
Documentar a definição de pronto para PR em relação a E2E (TST-07): o que deve passar localmente, o que é opcional (Google), e como isso se relaciona com CI ausente ou futuro.

Purpose: Fechar a iniciativa TST com critérios explícitos de merge.
Output: seção final no `tests/e2e/README.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TESTING-INITIATIVE.md
@tests/e2e/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seção Portão de PR / CI no README</name>
  <files>tests/e2e/README.md</files>
  <read_first>
    - tests/e2e/README.md
    - .planning/TESTING-INITIATIVE.md (item TST-07)
    - Glob .github/workflows (confirmar se diretório existe)
  </read_first>
  <action>
Adicionar seção `## Portão de PR (E2E)` ao `tests/e2e/README.md` com checklist markdown de pelo menos 4 itens `- [ ]`, incluindo **obrigatoriamente** os temas abaixo (redação livre, mas todos os conceitos devem aparecer como palavras-chave literais em algum lugar da seção):

1. `npm run test:e2e:smoke` com `E2E_AUTH_MODE=password` (ou não definido) passando localmente antes do merge.
2. `supabase db reset` / seed aplicado quando o falhanço for de dados.
3. Execução Google (`npm run test:e2e:google` ou equivalente documentado) marcada como **recomendada** ou **obrigatória para mudanças em auth/OAuth** — escolher uma política explícita e escrever em uma frase.
4. CI: se **não** existir pasta `.github/workflows/` no repositório, declarar literalmente: `Nenhum workflow GitHub Actions de E2E está versionado neste repositório.` Se existir, em vez disso listar o nome do arquivo yaml relevante.

Incluir nota de que login Google real em CI pode exigir segredos e job opcional (D-07 do contexto).
  </action>
  <verify>
    <automated>grep -q "## Portão de PR (E2E)" tests/e2e/README.md && grep -q "npm run test:e2e:smoke" tests/e2e/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Heading exato `## Portão de PR (E2E)` presente
    - Seção contém substring `npm run test:e2e:smoke` e menção explícita a `E2E_AUTH_MODE=password` **ou** frase equivalente "modo password" + "smoke" na mesma seção
    - Checklist com pelo menos 4 itens `- [ ]`
    - Ou a frase literal `Nenhum workflow GitHub Actions de E2E está versionado neste repositório.` **ou** referência a um arquivo `.github/workflows/*.yml` existente
  </acceptance_criteria>
  <done>TST-07 documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / segredos | Documentação não armazena secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-05 | Elevation of Privilege | Sugerir merge sem smoke | mitigate | Checklist exige smoke password local explícito |
</threat_model>

<verification>
```bash
grep -q "## Portão de PR (E2E)" tests/e2e/README.md
```
</verification>
