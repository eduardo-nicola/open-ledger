---
phase: 01.1-e2e-clarity-and-test-confidence-tst
verified: 2026-04-22T23:50:00Z
status: human_needed
score: 11/11 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Executar `npm run test:e2e:smoke` com supabase rodando e seed aplicado"
    expected: "Suíte verde — todos os testes @smoke passam com E2E_AUTH_MODE=password (ou ausente)"
    why_human: "Requer supabase start + supabase db reset + servidor Next.js rodando; não executável no verificador estático"
  - test: "Verificar fluxo Google OAuth real — Variação A ou B conforme descrito no README"
    expected: "Setup chega em /profile com storageState gravado; testes @smoke subsequentes passam com modo google"
    why_human: "Requer sessão Google real, browser headed e conta de teste sem 2FA; não automatizável estaticamente"
---

# Phase 01.1: E2E Clarity and Test Confidence Verification Report

**Phase Goal:** Reduzir variações confusas nos testes E2E; documentar Google OAuth real (conta de teste sem 2FA) com duas variações (Google já logado vs login completo); em seguida executar a suíte já existente; alinhar tags com rastreabilidade (ver `.planning/TESTING-INITIATIVE.md` e `01.1-CONTEXT.md`).
**Verified:** 2026-04-22T23:50:00Z
**Status:** human_needed
**Re-verification:** Não — verificação inicial

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | REQUIREMENTS.md lista TST-01 a TST-07 com texto alinhado a TESTING-INITIATIVE.md | ✓ VERIFIED | `grep -q "### E2E testing initiative" .planning/REQUIREMENTS.md` ✓; 7 itens `- [ ]` TST presentes; nenhum marcado como `[x]` |
| 2 | tests/e2e/README.md existe com matriz Variação A/B × comando npm × ambiente | ✓ VERIFIED | Arquivo existe; contém "Variação A", "Variação B", tabela com 5 colunas e 4 linhas de cenário |
| 3 | README declara Google OAuth real como caminho de confiança e password/API como debug | ✓ VERIFIED | Heading `## Caminho de confiança (Google OAuth real)` + `## Modo password (apenas debug / velocidade)` |
| 4 | .env.example menciona E2E_AUTH_MODE e aponta para tests/e2e/README.md | ✓ VERIFIED | `grep -q "Documentacao completa: tests/e2e/README.md" .env.example` ✓; `E2E_AUTH_MODE` comentado em 3 linhas |
| 5 | Nenhum par test.title duplica o mesmo token @auth-04 para cenários distintos | ✓ VERIFIED | `grep -E -c '@smoke @auth-04 -' auth.spec.ts` → 0; tags @auth-04a…d aplicadas em 4 testes distintos |
| 6 | README contém tabela mapeando @auth-04a…d para sub-capacidades de AUTH-04 | ✓ VERIFIED | Seção `## Tags e rastreio (AUTH)` com 7 linhas: @auth-01, @auth-02, @auth-03, @auth-04a, @auth-04b, @auth-04c, @auth-04d |
| 7 | README explica diferença entre test:e2e:auth e test:e2e:smoke em termos de grep | ✓ VERIFIED | Seção `## Scripts npm e grep` com explicação de `--grep @smoke` vs `--grep @auth` e relação de superconjunto |
| 8 | auth.setup.ts com prefixo E2E_SETUP_PREFIX em todos os erros e mensagens acionáveis | ✓ VERIFIED | `const E2E_SETUP_PREFIX = '[E2E setup]'` definida; todos os 7 `throw new Error` interpolam `${E2E_SETUP_PREFIX}`; mensagens incluem `supabase db reset`, `test@open-ledger.local`, `tests/e2e/README.md` |
| 9 | Mensagem de timeout em modo google não sugere 2FA como causa principal quando credenciais ausentes | ✓ VERIFIED | Bifurcação explícita: se `E2E_AUTH_MODE=google` e sem credenciais → mensagem específica sem mencionar 2FA; 2FA aparece apenas entre parênteses no timeout genérico: `"útil em redes lentas ou com 2FA"` |
| 10 | README define checklist mínima de PR para E2E (smoke password + Google + CI) | ✓ VERIFIED | Heading `## Portão de PR (E2E)` com 5 itens `- [ ]`; `npm run test:e2e:smoke` com `E2E_AUTH_MODE=password` como gate obrigatório |
| 11 | README declara ausência de workflow GitHub Actions ou aponta arquivo existente | ✓ VERIFIED | Frase literal: `"Nenhum workflow GitHub Actions de E2E está versionado neste repositório."`; `.github/workflows/` não existe no repositório |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/e2e/README.md` | Contrato de execução E2E — contém "Variação A" | ✓ VERIFIED | Criado em commit `3500674`; 228 linhas; 8+ seções; substantivo e referenciado |
| `.planning/REQUIREMENTS.md` | Rastreio formal TST-* — contém "TST-01" | ✓ VERIFIED | Seção `### E2E testing initiative (Phase 01.1)` adicionada em commit `ef3ea18` |
| `tests/e2e/auth.spec.ts` | Tags 1:1 por cenário — contém "@auth-04a" | ✓ VERIFIED | 6 testes: @auth-01, @auth-02, @auth-04a, @auth-04b, @auth-04c, @auth-04d; commit `f76e512` |
| `tests/e2e/auth.setup.ts` | Mensagens acionáveis — contém "[E2E setup]" | ✓ VERIFIED | `E2E_SETUP_PREFIX` definida e usada em todos os 7 throws; commit `38180c9` |
| `.env.example` | Ref ao README E2E | ✓ VERIFIED | Linha `# Documentacao completa: tests/e2e/README.md` presente; commit `33a80aa` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.env.example` | `tests/e2e/README.md` | comentário de documentação | ✓ WIRED | Linha `# Documentacao completa: tests/e2e/README.md` conecta o arquivo de env ao contrato |
| `tests/e2e/auth.setup.ts` | `tests/e2e/README.md` | mensagem de erro | ✓ WIRED | Throw de env ausente cita `tests/e2e/README.md` literalmente |
| `tests/e2e/auth.setup.ts` | `E2E_GOOGLE_TEST_EMAIL` / `E2E_GOOGLE_TEST_PASSWORD` | verificação de bifurcação no timeout | ✓ WIRED | Código verifica presença das variáveis antes de emitir erro específico para modo google |
| `tests/e2e/README.md` | `.planning/TESTING-INITIATIVE.md` | rodapé de referência | ✓ WIRED | `*Referência da iniciativa: .planning/TESTING-INITIATIVE.md*` |
| `REQUIREMENTS.md` | Phase 01.1 | heading de seção | ✓ WIRED | `### E2E testing initiative (Phase 01.1)` liga os TSTs à fase do roadmap |

---

### Data-Flow Trace (Level 4)

**SKIPPED** — Fase exclusivamente de documentação e metadados de teste. Nenhum componente de UI ou endpoint de API que renderize dados dinâmicos foi criado. Arquivos modificados são: markdown docs, arquivos TypeScript de configuração de setup (sem renderização de dados), e arquivo de ambiente.

---

### Behavioral Spot-Checks

| Comportamento | Comando | Resultado | Status |
|---------------|---------|-----------|--------|
| README existe e tem 127.0.0.1:3000 | `test -f tests/e2e/README.md && grep -q "127.0.0.1:3000" tests/e2e/README.md` | OK | ✓ PASS |
| Tags @auth-04a…d presentes sem ambiguidade | `grep -q "@auth-04a" auth.spec.ts && grep -E -c "@smoke @auth-04 -" auth.spec.ts` | @auth-04a ✓ / count 0 | ✓ PASS |
| E2E_SETUP_PREFIX definida e usada | `grep -q "E2E_SETUP_PREFIX = '\[E2E setup\]'" auth.setup.ts` | OK | ✓ PASS |
| .env.example referencia README | `grep -q "Documentacao completa: tests/e2e/README.md" .env.example` | OK | ✓ PASS |
| npm run test:e2e:smoke (suíte real) | Requer supabase local + servidor | — | ? SKIP |
| npm run test:e2e:google (Google OAuth) | Requer browser headed + sessão Google | — | ? SKIP |

---

### Requirements Coverage

| Requisito | Plano | Descrição | Status | Evidência |
|-----------|-------|-----------|--------|-----------|
| TST-01 | 01.1-01 | Documentação README com pré-requisitos, variáveis e matriz | ✓ SATISFIED | tests/e2e/README.md existe com todas as seções exigidas |
| TST-02 | 01.1-01 | Variações A/B Google OAuth documentadas passo a passo | ✓ SATISFIED | Seções `## Variação A` e `## Variação B` com passos numerados |
| TST-03 | 01.1-01 | Modo password explícito como secundário/debug | ✓ SATISFIED | `## Modo password (apenas debug / velocidade)` com limitações documentadas |
| TST-04 | 01.1-02 | Tags @smoke com ID único por cenário (sufixos @auth-04a…d) | ✓ SATISFIED | auth.spec.ts sem `@auth-04` duplicado; 4 tags granulares em vigor |
| TST-05 | 01.1-02 | Semântica de test:e2e:auth vs test:e2e:smoke documentada | ✓ SATISFIED | `## Scripts npm e grep` explica `--grep @smoke` vs `--grep @auth` e relação de superconjunto |
| TST-06 | 01.1-03 | Mensagens de erro acionáveis no auth.setup | ✓ SATISFIED | 7 throws com prefixo [E2E setup]; mensagens específicas para env ausente, seed e google sem credenciais |
| TST-07 | 01.1-04 | Definição de pronto para PR com checklist E2E | ✓ SATISFIED | `## Portão de PR (E2E)` com 5 itens de checklist e declaração de CI |

**Observação:** A tabela de rastreabilidade no final de `REQUIREMENTS.md` não inclui os TST-01…TST-07 (seção adicionada após a tabela inicial). Isso é cosmético — não afeta a cobertura real pois a seção `### E2E testing initiative` está presente e vinculada à Phase 01.1 pelo heading.

---

### Anti-Patterns Found

| Arquivo | Linha | Padrão | Severidade | Impacto |
|---------|-------|--------|------------|---------|
| — | — | Nenhum TODO/FIXME/PLACEHOLDER encontrado | — | — |
| — | — | Nenhum TST marcado `[x]` (correto — implementação não é escopo desta fase) | — | — |
| `auth.setup.ts` | 62 | 2FA mencionado no timeout genérico | ℹ️ Info | Aceitável — aparece entre parênteses como causa secundária (`"útil em redes lentas ou com 2FA"`), não como causa principal; a mensagem específica do google sem credenciais não menciona 2FA |

---

### Human Verification Required

#### 1. Execução da Suíte E2E (modo password)

**Test:** Rodar `npm run test:e2e:smoke` com supabase local ativo e seed aplicado:
```bash
supabase start
supabase db reset
# Em outro terminal:
npm run dev
# Em outro terminal:
npm run test:e2e:smoke
```
**Expected:** Todos os 6 testes @smoke passam (2 auth + 4 auth-04a…d). Zero falhas.
**Why human:** Requer supabase start (Docker), servidor Next.js na porta 3000, e seed do usuário `test@open-ledger.local`. Não é possível executar em verificação estática.

#### 2. Fluxo Google OAuth Real (Variação A ou B)

**Test:** Executar `npm run test:e2e:google` com conta de teste Google sem 2FA, conforme descrito no README:
- Variação A: conta Google já logada no browser → setup deve chegar em `/profile` sem redigitar credenciais
- Variação B: login completo com `E2E_GOOGLE_TEST_EMAIL` e `E2E_GOOGLE_TEST_PASSWORD` definidos → automação preenche campos
**Expected:** Setup grava `tests/.auth/user.json`; testes @smoke subsequentes passam com modo google.
**Why human:** Requer sessão Google real, browser headed, conta sem 2FA e (opcionalmente) credenciais de teste. O comportamento de redirect e cookie cross-origin (127.0.0.1 vs localhost) só é validável em execução real.

---

### Gaps Summary

Nenhum gap encontrado. Todos os 11 must-haves foram verificados programaticamente. Os 7 requisitos TST-01 a TST-07 estão cobertos pelos artefatos existentes.

Os 2 itens de verificação humana são inerentes à natureza da fase — a execução da suíte e o fluxo Google OAuth real não podem ser verificados de forma estática. Eles confirmam a integração end-to-end que a documentação descreve.

---

_Verified: 2026-04-22T23:50:00Z_
_Verifier: Claude (gsd-verifier)_
