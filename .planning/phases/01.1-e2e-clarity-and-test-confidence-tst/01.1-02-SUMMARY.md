---
phase: 01.1-e2e-clarity-and-test-confidence-tst
plan: 2
subsystem: testing
tags: [playwright, e2e, tags, documentation, traceability]
dependency_graph:
  requires:
    - "01.1-01"
  provides:
    - "Rastreabilidade 1:1 requisito ↔ tag Playwright para AUTH"
  affects:
    - tests/e2e/auth.spec.ts
    - tests/e2e/README.md
tech_stack:
  added: []
  patterns:
    - "Sufixos semânticos em tags Playwright (auth-04a…d) para granularidade 1:1"
key_files:
  modified:
    - tests/e2e/auth.spec.ts
    - tests/e2e/README.md
decisions:
  - "Tags @auth-04a…d substituem o tag ambíguo @auth-04 para mapear 1:1 cada sub-cenário de AUTH-04"
  - "@auth-03 mantido em rls.spec.ts fora do escopo deste plano"
metrics:
  duration: "~8 minutos"
  completed: "2026-04-22T23:16:14Z"
  tasks: 2
  files: 2
---

# Phase 01.1 Plan 02: Higiene de tags Playwright e documentação de scripts E2E

**One-liner:** Tags `@auth-04a…d` com sufixos granulares em `auth.spec.ts` + tabela de rastreabilidade AUTH no README com semântica de `--grep @smoke` vs `--grep @auth`.

---

## Tasks Executadas

| Task | Nome | Commit | Arquivos |
|------|------|--------|----------|
| 1 | Renomear tags @auth-04 duplicadas em auth.spec.ts | f76e512 | tests/e2e/auth.spec.ts |
| 2 | Documentar tags e scripts no README | fc07c52 | tests/e2e/README.md |

---

## Resultados

### Task 1 — Renomear tags @auth-04

Quatro testes que compartilhavam o mesmo token `@auth-04` receberam sufixos semânticos:

| Antes | Depois | Cenário |
|-------|--------|---------|
| `@smoke @auth-04 - tela de perfil exibe nome e email do usuario` | `@smoke @auth-04a - …` | Nome/email/botão visíveis |
| `@smoke @auth-04 - tela de perfil nao contem inputs de edicao (read-only)` | `@smoke @auth-04b - …` | Read-only sem inputs |
| `@smoke @auth-04 - botao logout abre AlertDialog de confirmacao` | `@smoke @auth-04c - …` | AlertDialog de logout |
| `@smoke @auth-04 - logout redireciona para /login` | `@smoke @auth-04d - …` | Redirect após logout |

Nenhum corpo de teste foi alterado — apenas strings de título. `@auth-01` e `@auth-02` permaneceram inalterados (um teste cada).

### Task 2 — Documentação no README

Duas seções adicionadas ao final de `tests/e2e/README.md`:

- **`## Tags e rastreio (AUTH)`**: tabela com 7 linhas mapeando `@auth-01`, `@auth-02`, `@auth-03` (rls.spec.ts), `@auth-04a…d` → requisito + arquivo + descrição do cenário.
- **`## Scripts npm e grep`**: explica `--grep @smoke` vs `--grep @auth`, relação de superconjunto, e inclui exemplo `rg '@auth-' tests/e2e`.

---

## Desvios do Plano

Nenhum — plano executado exatamente como especificado.

---

## Known Stubs

Nenhum — este plano afeta apenas metadados de título de teste e documentação.

---

## Threat Flags

Nenhum — alterações limitadas a strings de título e markdown de documentação; sem novos endpoints, paths de auth ou acesso a dados.

---

## Self-Check: PASSED

| Item | Status |
|------|--------|
| tests/e2e/auth.spec.ts existe | ✓ FOUND |
| tests/e2e/README.md existe | ✓ FOUND |
| 01.1-02-SUMMARY.md existe | ✓ FOUND |
| commit f76e512 (task 1) | ✓ FOUND (`git log --oneline -5`) |
| commit fc07c52 (task 2) | ✓ FOUND (`git log --oneline -5`) |
