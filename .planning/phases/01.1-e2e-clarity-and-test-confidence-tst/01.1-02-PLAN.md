---
phase: 01.1-e2e-clarity-and-test-confidence-tst
plan: 2
type: execute
wave: 2
depends_on:
  - "01.1-01"
files_modified:
  - tests/e2e/auth.spec.ts
  - tests/e2e/README.md
autonomous: true
requirements:
  - TST-04
  - TST-05

must_haves:
  truths:
    - "Nenhum par test.title duplica o mesmo token @auth-04 isolado para cenários distintos"
    - "README contém tabela mapeando @auth-04a… para sub-capacidades de AUTH-04"
    - "README explica diferença entre npm run test:e2e:auth e test:e2e:smoke em termos de grep"
  artifacts:
    - path: "tests/e2e/auth.spec.ts"
      provides: "Tags 1:1 por cenário"
      contains: "@auth-04a"
    - path: "tests/e2e/README.md"
      provides: "Rastreabilidade AUTH ↔ tags"
      contains: "@auth-04a"
---

<objective>
Corrigir higiene de tags Playwright (TST-04) e documentar semântica dos scripts `test:e2e:auth` vs `test:e2e:smoke` (TST-05), sem alterar asserções funcionais dos testes.

Purpose: Permitir rastrear requisito ↔ teste sem ambiguidade.
Output: `auth.spec.ts` com sufixos granulares + seção nova no README.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/e2e/auth.spec.ts
@package.json
@.planning/REQUIREMENTS.md
@tests/e2e/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renomear tags @auth-04 duplicadas em auth.spec.ts</name>
  <files>tests/e2e/auth.spec.ts</files>
  <read_first>
    - tests/e2e/auth.spec.ts (cinco testes @smoke atuais)
    - .planning/REQUIREMENTS.md (definições AUTH-01 … AUTH-04)
  </read_first>
  <action>
Editar **apenas** a string do primeiro argumento de cada `test(...)` em `tests/e2e/auth.spec.ts`:

1. Manter o primeiro teste como contém `@auth-01` (não alterar o identificador de requisito deste cenário).

2. Manter o segundo teste com `@auth-02`.

3. Para os quatro testes que hoje começam com `@smoke @auth-04`, substituir **exatamente** pelos seguintes prefixos de título (restante do título pode permanecer igual ou ser levemente ajustado para legibilidade):
   - `@smoke @auth-04a` — cenário que valida nome + email + botão sair visíveis
   - `@smoke @auth-04b` — cenário read-only (sem inputs)
   - `@smoke @auth-04c` — cenário AlertDialog de logout
   - `@smoke @auth-04d` — cenário redirect para `/login` após logout

4. Garantir que **não** exista mais a substring `@auth-04 -` (espaço hífen espaço após auth-04) nos títulos — use apenas @auth-04a / b / c / d.

5. Não alterar corpo dos testes (apenas strings de título).
  </action>
  <verify>
    <automated>test "$(grep -E -c '@smoke @auth-04 -' tests/e2e/auth.spec.ts || echo 0)" -eq 0 && grep -q '@auth-04a' tests/e2e/auth.spec.ts && grep -q '@auth-04d' tests/e2e/auth.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/auth.spec.ts` contém `@auth-04a`, `@auth-04b`, `@auth-04c`, `@auth-04d` cada um em exatamente uma linha de título de teste
    - Nenhuma linha contém o padrão `@smoke @auth-04 -` (grep -E retorna vazio)
    - `@auth-01` e `@auth-02` permanecem em exatamente um teste cada
  </acceptance_criteria>
  <done>Tags granulares para AUTH-04 aplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar tags e scripts no README</name>
  <files>tests/e2e/README.md</files>
  <read_first>
    - tests/e2e/README.md (conteúdo após plano 01)
    - package.json (linhas dos scripts e2e)
    - tests/e2e/auth.spec.ts (títulos finais com tags)
  </read_first>
  <action>
Adicionar ao final de `tests/e2e/README.md` (antes do rodapé de referência à iniciativa, ou integrando-o) duas seções:

## Tags e rastreio (AUTH)

Tabela markdown obrigatória com colunas: `Tag`, `Requisito`, `Arquivo`, `O que verifica`.

Linhas obrigatórias (texto da coluna "O que verifica" pode ser resumido):

| Tag | Requisito | Arquivo | O que verifica |
|-----|-----------|---------|----------------|
| @auth-01 | AUTH-01 | tests/e2e/auth.spec.ts | (descrever o cenário atual pós-setup) |
| @auth-02 | AUTH-02 | tests/e2e/auth.spec.ts | persistência após reload |
| @auth-04a | AUTH-04 | tests/e2e/auth.spec.ts | nome/email visíveis |
| @auth-04b | AUTH-04 | tests/e2e/auth.spec.ts | perfil sem inputs |
| @auth-04c | AUTH-04 | tests/e2e/auth.spec.ts | diálogo de confirmação de logout |
| @auth-04d | AUTH-04 | tests/e2e/auth.spec.ts | redirect /login após logout |

Mencionar que `@auth-03` vive em `tests/e2e/rls.spec.ts` (não inventar tags novas lá neste plano).

## Scripts npm e grep

Explicar em 2–4 frases:

- `npm run test:e2e:smoke` executa `playwright test --grep @smoke` — subconjunto marcado smoke.
- `npm run test:e2e:auth` executa `playwright test --grep @auth` — todos os testes cujo título contém `@auth` (inclui @auth-04a porque o grep casa substring).
- Ambos respeitam o mesmo `storageState` / modo de auth definido por `E2E_AUTH_MODE` no momento da execução.

Incluir linha literal de exemplo de comando para inspecionar tags: `rg '@auth-' tests/e2e` (ou `grep -R` se preferir POSIX).
  </action>
  <verify>
    <automated>grep -q "## Tags e rastreio" tests/e2e/README.md && grep -q "@auth-04a" tests/e2e/README.md && grep -q "test:e2e:auth" tests/e2e/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/README.md` contém heading exato `## Tags e rastreio (AUTH)`
    - A tabela inclui a substring `@auth-04a` e `@auth-04d`
    - Existe parágrafo ou lista que menciona explicitamente `--grep @smoke` e `--grep @auth` (substrings literais)
  </acceptance_criteria>
  <done>TST-04 e TST-05 cobertos na documentação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tags públicas | Apenas metadados de teste; sem credenciais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03 | Tampering | grep @auth em CI | accept | Tags são contrato de seleção; mudanças revisadas em PR |
</threat_model>

<verification>
```bash
test "$(grep -E -c '@smoke @auth-04 -' tests/e2e/auth.spec.ts || echo 0)" -eq 0
grep -q "## Tags e rastreio (AUTH)" tests/e2e/README.md
npm run test:e2e:smoke
npm run test:e2e:auth
```
</verification>
