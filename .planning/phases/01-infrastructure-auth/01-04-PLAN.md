---
phase: 01-infrastructure-auth
plan: 04
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - app/(auth)/layout.tsx
  - app/(auth)/login/page.tsx
  - app/(app)/layout.tsx
  - app/(app)/profile/page.tsx
  - components/layout/header.tsx
  - components/layout/sidebar.tsx
  - components/layout/bottom-nav.tsx
  - components/auth/logout-button.tsx
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "Tela de login exibe logo 'Open Ledger', tagline e botão 'Entrar com Google' centralizado"
    - "Botão Google inicia fluxo OAuth e fica desabilitado/loading durante redirect"
    - "App shell mobile-first: header 56px + conteúdo + bottom nav 64px (mobile)"
    - "App shell desktop: sidebar 240px (esquerda) + conteúdo (direita)"
    - "Nav possui 4 itens: Dashboard, Contas, Transações, Perfil — apenas Perfil ativo na Fase 1"
    - "Tela de perfil exibe avatar Google (80px circular), nome completo e email"
    - "Botão 'Sair da conta' abre AlertDialog de confirmação antes de fazer logout"
    - "export const dynamic = 'force-dynamic' em profile/page.tsx"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "Tela de login com botão Google OAuth"
      contains: "signInWithOAuth"
    - path: "app/(app)/layout.tsx"
      provides: "App Shell com header/sidebar/bottom-nav"
      contains: "AppShell"
    - path: "app/(app)/profile/page.tsx"
      provides: "Tela de perfil read-only com avatar, nome e logout"
      contains: "force-dynamic"
    - path: "components/layout/header.tsx"
      provides: "Header mobile (56px) com logo e avatar do usuário"
      exports: ["Header"]
    - path: "components/layout/sidebar.tsx"
      provides: "Sidebar desktop (240px) com navegação"
      exports: ["Sidebar"]
    - path: "components/layout/bottom-nav.tsx"
      provides: "Bottom navigation mobile (64px) com 4 itens"
      exports: ["BottomNav"]
    - path: "components/auth/logout-button.tsx"
      provides: "Botão logout com AlertDialog de confirmação"
      exports: ["LogoutButton"]
  key_links:
    - from: "app/(app)/profile/page.tsx"
      to: "lib/supabase/server.ts"
      via: "createClient() → supabase.auth.getUser()"
      pattern: "getUser"
    - from: "app/(auth)/login/page.tsx"
      to: "lib/supabase/client.ts"
      via: "createClient() → supabase.auth.signInWithOAuth()"
      pattern: "signInWithOAuth"
    - from: "components/auth/logout-button.tsx"
      to: "lib/supabase/client.ts"
      via: "createClient() → supabase.auth.signOut()"
      pattern: "signOut"
---

<objective>
Implementar as três telas da Fase 1 seguindo o UI-SPEC.md exatamente: tela de login com Google OAuth, app shell mobile-first (header + sidebar + bottom nav), e tela de perfil read-only com logout.

Purpose: AUTH-04 requer que o usuário visualize seu perfil (nome + avatar do Google). As telas de login e app shell são a experiência completa de auth que o usuário vai interagir.
Output: Três telas funcionais e estilizadas conforme UI-SPEC. App compilando e navegável localmente.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-auth/01-UI-SPEC.md
@.planning/phases/01-infrastructure-auth/01-CONTEXT.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
</context>

<interfaces>
<!-- Imports disponíveis para o executor deste plano -->

```typescript
// Auth — Server side
import { createClient } from '@/lib/supabase/server'
const supabase = await createClient()
const { data: { user } } = await supabase.auth.getUser()
// user.user_metadata.full_name  → string (nome Google)
// user.user_metadata.avatar_url → string (URL avatar Google)
// user.email                    → string

// Auth — Client side
import { createClient } from '@/lib/supabase/client'
const supabase = createClient()
await supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: `${window.location.origin}/auth/callback` } })
await supabase.auth.signOut()

// shadcn/ui components disponíveis
import { Button } from '@/components/ui/button'
import { Avatar, AvatarImage, AvatarFallback } from '@/components/ui/avatar'
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog'
import { Separator } from '@/components/ui/separator'
import { Skeleton } from '@/components/ui/skeleton'
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip'

// lucide-react icons para navegação
import { LayoutDashboard, Landmark, ArrowLeftRight, User } from 'lucide-react'
```

Tokens de cor do UI-SPEC (CSS variables shadcn zinc/dark):
- background: `bg-background` (#09090b)
- card/sidebar/nav: `bg-card` (#18181b)
- primary/accent: `bg-primary` / `text-primary` (#22c55e verde)
- muted text: `text-muted-foreground` (#a1a1aa)
- destructive: `bg-destructive` / `text-destructive` (logout)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tela de Login — layout do grupo auth + página de login com Google OAuth</name>
  <files>app/(auth)/layout.tsx, app/(auth)/login/page.tsx</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-UI-SPEC.md (seção "Tela de Login — Especificação Visual", "Color", "Typography", "Copywriting Contract", "Interações & Estados", "Acessibilidade — Requisitos Mínimos")
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (seção "Login Page — Botão Google OAuth" — código de referência; "Open Questions" sobre raw_user_meta_data)
  </read_first>

  <action>
**1. Criar `app/(auth)/layout.tsx`** — layout para rotas não-autenticadas:

```typescript
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen bg-background">
      {children}
    </div>
  )
}
```

**2. Criar `app/(auth)/login/page.tsx`** seguindo o UI-SPEC exatamente:

```typescript
'use client'

import { useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'

// Ícone SVG oficial do Google (não usar lucide-react para isso)
function GoogleIcon() {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 24 24"
      width="18"
      height="18"
      aria-hidden="true"
    >
      <path
        d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
        fill="#4285F4"
      />
      <path
        d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
        fill="#34A853"
      />
      <path
        d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"
        fill="#FBBC05"
      />
      <path
        d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
        fill="#EA4335"
      />
    </svg>
  )
}

export default function LoginPage() {
  const [isLoading, setIsLoading] = useState(false)

  async function handleGoogleLogin() {
    setIsLoading(true)
    try {
      const supabase = createClient()
      await supabase.auth.signInWithOAuth({
        provider: 'google',
        options: {
          redirectTo: `${window.location.origin}/auth/callback`,
        },
      })
      // Não resetar isLoading — página vai redirecionar
    } catch {
      setIsLoading(false)
    }
  }

  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <div className="w-full max-w-[360px] flex flex-col items-center gap-12">
        {/* Identidade do app — UI-SPEC: Display 28px semibold */}
        <div className="flex flex-col items-center gap-2">
          {/* Ícone do app — UI-SPEC: 40px, cor primary (verde) */}
          <div
            className="h-10 w-10 rounded-full bg-primary"
            aria-hidden="true"
          />
          <h1 className="text-[28px] font-semibold leading-[1.1] text-foreground">
            Open Ledger
          </h1>
          <p className="text-sm text-muted-foreground">
            Suas finanças em um só lugar
          </p>
        </div>

        {/* CTA Section */}
        <div className="w-full flex flex-col gap-3">
          <Button
            variant="outline"
            className="w-full h-11 gap-2"
            onClick={handleGoogleLogin}
            disabled={isLoading}
            aria-label="Entrar com Google"
          >
            {isLoading ? (
              <span className="h-4 w-4 animate-spin rounded-full border-2 border-current border-t-transparent" />
            ) : (
              <GoogleIcon />
            )}
            {isLoading ? 'Carregando…' : 'Entrar com Google'}
          </Button>

          {/* Legal footer — UI-SPEC: Body 14px, muted */}
          <p className="text-xs text-center text-muted-foreground">
            Ao continuar, você concorda com os Termos de Uso.
          </p>
        </div>
      </div>
    </main>
  )
}
```

**Checklist de conformidade com UI-SPEC:**
- [x] Fundo: `bg-background` (via min-h-screen, sem card wrapper — UI-SPEC: "não usar card")
- [x] Logo: círculo `bg-primary` (verde) 40px
- [x] Título "Open Ledger": Display 28px semibold (text-[28px] font-semibold)
- [x] Tagline: "Suas finanças em um só lugar" (texto exato do UI-SPEC)
- [x] Botão: `variant="outline"`, w-100%, h-11 (44px mínimo), ícone Google SVG
- [x] CTA text: "Entrar com Google" (copywriting exato)
- [x] Estado loading: spinner + "Carregando…" + botão disabled
- [x] Footer legal: "Ao continuar, você concorda com os Termos de Uso."
- [x] aria-label no botão (acessibilidade)
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10; test -f "app/(auth)/login/page.tsx" && grep -q "signInWithOAuth" "app/(auth)/login/page.tsx" && grep -q "Entrar com Google" "app/(auth)/login/page.tsx" && grep -q "aria-label" "app/(auth)/login/page.tsx" && echo "OK: login page criada com OAuth e acessibilidade"</automated>
  </verify>

  <acceptance_criteria>
    - app/(auth)/layout.tsx existe
    - app/(auth)/login/page.tsx existe
    - login/page.tsx contém `'use client'`
    - login/page.tsx contém `signInWithOAuth` com `provider: 'google'`
    - login/page.tsx contém `redirectTo` apontando para `/auth/callback`
    - login/page.tsx contém texto "Entrar com Google" (copywriting exato do UI-SPEC)
    - login/page.tsx contém texto "Open Ledger" (nome do app)
    - login/page.tsx contém texto "Suas finanças em um só lugar" (tagline exata)
    - login/page.tsx contém texto "Ao continuar, você concorda com os Termos de Uso." (footer legal exato)
    - login/page.tsx contém `aria-label="Entrar com Google"` (acessibilidade)
    - login/page.tsx contém `disabled={isLoading}` (estado de loading)
    - login/page.tsx contém `variant="outline"` no Button
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>

  <done>Tela de login criada com botão Google OAuth, estado de loading, copywriting do UI-SPEC e acessibilidade. Layout do route group auth criado.</done>
</task>

<task type="auto">
  <name>Task 2: App Shell — layout do grupo app com header mobile, sidebar desktop e bottom nav</name>
  <files>app/(app)/layout.tsx, components/layout/header.tsx, components/layout/sidebar.tsx, components/layout/bottom-nav.tsx</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-UI-SPEC.md (seção "Layout — App Shell (Mobile-first)" — diagramas ASCII e especificações de h=56px, sidebar 240px, bottom nav 64px; "Copywriting Contract" para labels de nav; "Interações & Estados" para nav items inativos)
    - lib/supabase/server.ts (createClient para buscar user no layout server-side)
  </read_first>

  <action>
**1. Criar `components/layout/header.tsx`** — header mobile (56px) com logo e avatar:

```typescript
import Link from 'next/link'
import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar'

interface HeaderProps {
  userName: string
  avatarUrl: string | null
}

export function Header({ userName, avatarUrl }: HeaderProps) {
  const initials = userName
    .split(' ')
    .map((n) => n[0])
    .slice(0, 2)
    .join('')
    .toUpperCase()

  return (
    <header className="h-14 bg-card border-b border-border flex items-center justify-between px-4 sticky top-0 z-50">
      {/* Logo — apenas mobile (sidebar tem logo no desktop) */}
      <span className="text-base font-semibold text-foreground md:hidden">
        Open Ledger
      </span>

      {/* Avatar — link para perfil */}
      <Link
        href="/profile"
        className="ml-auto rounded-full focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
        aria-label={userName}
      >
        <Avatar className="h-8 w-8">
          <AvatarImage
            src={avatarUrl ?? undefined}
            alt={userName}
          />
          <AvatarFallback className="text-xs bg-muted text-muted-foreground">
            {initials}
          </AvatarFallback>
        </Avatar>
      </Link>
    </header>
  )
}
```

**2. Criar `components/layout/sidebar.tsx`** — sidebar desktop (240px):

```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { LayoutDashboard, Landmark, ArrowLeftRight, User } from 'lucide-react'
import { cn } from '@/lib/utils'
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from '@/components/ui/tooltip'

const navItems = [
  { href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard, activeInPhase1: false },
  { href: '/accounts', label: 'Contas', icon: Landmark, activeInPhase1: false },
  { href: '/transactions', label: 'Transações', icon: ArrowLeftRight, activeInPhase1: false },
  { href: '/profile', label: 'Perfil', icon: User, activeInPhase1: true },
] as const

export function Sidebar() {
  const pathname = usePathname()

  return (
    <aside className="hidden md:flex w-60 min-h-screen bg-card border-r border-border flex-col py-4">
      {/* Logo */}
      <div className="px-4 mb-6">
        <span className="text-base font-semibold text-foreground">Open Ledger</span>
      </div>

      {/* Navegação */}
      <TooltipProvider>
        <nav className="flex-1 flex flex-col gap-1 px-2" aria-label="Navegação principal">
          {navItems.map(({ href, label, icon: Icon, activeInPhase1 }) => {
            const isActive = pathname.startsWith(href)

            if (!activeInPhase1) {
              return (
                <Tooltip key={href}>
                  <TooltipTrigger asChild>
                    <div
                      className={cn(
                        'flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium',
                        'opacity-50 cursor-not-allowed text-muted-foreground'
                      )}
                      aria-disabled="true"
                    >
                      <Icon className="h-4 w-4 shrink-0" aria-hidden="true" />
                      <span>{label}</span>
                    </div>
                  </TooltipTrigger>
                  <TooltipContent side="right">
                    <p>Em breve — disponível nas próximas fases</p>
                  </TooltipContent>
                </Tooltip>
              )
            }

            return (
              <Link
                key={href}
                href={href}
                className={cn(
                  'flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium transition-colors',
                  isActive
                    ? 'bg-primary/10 text-primary'
                    : 'text-muted-foreground hover:bg-muted hover:text-foreground'
                )}
                aria-current={isActive ? 'page' : undefined}
              >
                <Icon className="h-4 w-4 shrink-0" aria-hidden="true" />
                <span>{label}</span>
              </Link>
            )
          })}
        </nav>
      </TooltipProvider>
    </aside>
  )
}
```

**3. Criar `components/layout/bottom-nav.tsx`** — navegação mobile (64px):

```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { LayoutDashboard, Landmark, ArrowLeftRight, User } from 'lucide-react'
import { cn } from '@/lib/utils'
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from '@/components/ui/tooltip'

const navItems = [
  { href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard, activeInPhase1: false },
  { href: '/accounts', label: 'Contas', icon: Landmark, activeInPhase1: false },
  { href: '/transactions', label: 'Transações', icon: ArrowLeftRight, activeInPhase1: false },
  { href: '/profile', label: 'Perfil', icon: User, activeInPhase1: true },
] as const

export function BottomNav() {
  const pathname = usePathname()

  return (
    <nav
      className="fixed bottom-0 left-0 right-0 h-16 bg-card border-t border-border md:hidden z-50"
      aria-label="Navegação principal"
    >
      <TooltipProvider>
        <ul className="h-full flex items-center justify-around px-2">
          {navItems.map(({ href, label, icon: Icon, activeInPhase1 }) => {
            const isActive = pathname.startsWith(href)

            if (!activeInPhase1) {
              return (
                <li key={href}>
                  <Tooltip>
                    <TooltipTrigger asChild>
                      <div
                        className="flex flex-col items-center gap-1 min-w-[44px] min-h-[44px] justify-center opacity-50 cursor-not-allowed"
                        aria-disabled="true"
                      >
                        <Icon className="h-5 w-5 text-muted-foreground" aria-hidden="true" />
                        <span className="text-[10px] text-muted-foreground">{label}</span>
                      </div>
                    </TooltipTrigger>
                    <TooltipContent side="top">
                      <p>Em breve — disponível nas próximas fases</p>
                    </TooltipContent>
                  </Tooltip>
                </li>
              )
            }

            return (
              <li key={href}>
                <Link
                  href={href}
                  className="flex flex-col items-center gap-1 min-w-[44px] min-h-[44px] justify-center"
                  aria-current={isActive ? 'page' : undefined}
                >
                  <Icon
                    className={cn(
                      'h-5 w-5',
                      isActive ? 'text-primary' : 'text-muted-foreground'
                    )}
                    aria-hidden="true"
                  />
                  <span
                    className={cn(
                      'text-[10px]',
                      isActive ? 'text-primary font-medium' : 'text-muted-foreground'
                    )}
                  >
                    {label}
                  </span>
                </Link>
              </li>
            )
          })}
        </ul>
      </TooltipProvider>
    </nav>
  )
}
```

**4. Criar `app/(app)/layout.tsx`** — App Shell server component que busca user e passa para os componentes:

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { Header } from '@/components/layout/header'
import { Sidebar } from '@/components/layout/sidebar'
import { BottomNav } from '@/components/layout/bottom-nav'

export default async function AppLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  const userName = (user.user_metadata?.full_name as string) ?? user.email ?? 'Usuário'
  const avatarUrl = (user.user_metadata?.avatar_url as string) ?? null

  return (
    <div className="flex min-h-screen bg-background">
      {/* Sidebar — visível apenas em md+ */}
      <Sidebar />

      {/* Área principal */}
      <div className="flex-1 flex flex-col min-w-0">
        {/* Header — visível em mobile (md: oculto via CSS pois sidebar tem o nome) */}
        <Header userName={userName} avatarUrl={avatarUrl} />

        {/* Conteúdo da página */}
        <main className="flex-1 p-6 md:p-8 pb-20 md:pb-8">
          {children}
        </main>
      </div>

      {/* Bottom Nav — visível apenas em mobile */}
      <BottomNav />
    </div>
  )
}
```

**NOTA:** `pb-20 md:pb-8` compensa a bottom nav fixa em mobile (h-16 = 64px ≈ pb-16, extra para segurança).
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10; test -f "app/(app)/layout.tsx" && test -f "components/layout/header.tsx" && test -f "components/layout/sidebar.tsx" && test -f "components/layout/bottom-nav.tsx" && grep -q "getUser" "app/(app)/layout.tsx" && grep -q "BottomNav" "app/(app)/layout.tsx" && echo "OK: app shell criado"</automated>
  </verify>

  <acceptance_criteria>
    - app/(app)/layout.tsx existe
    - app/(app)/layout.tsx contém `supabase.auth.getUser()` (não getSession)
    - app/(app)/layout.tsx contém `redirect('/login')` quando !user
    - app/(app)/layout.tsx importa e usa Header, Sidebar e BottomNav
    - app/(app)/layout.tsx lê `user.user_metadata?.full_name` e `user.user_metadata?.avatar_url`
    - components/layout/header.tsx existe e exporta `Header`
    - components/layout/header.tsx contém `AvatarImage` e `AvatarFallback` (shadcn Avatar)
    - components/layout/sidebar.tsx existe e exporta `Sidebar`
    - components/layout/sidebar.tsx contém `'use client'`
    - components/layout/sidebar.tsx contém `aria-current={isActive ? 'page' : undefined}` nos links ativos
    - components/layout/sidebar.tsx contém `opacity-50 cursor-not-allowed` nos itens inativos
    - components/layout/sidebar.tsx contém texto "Em breve — disponível nas próximas fases" no Tooltip
    - components/layout/bottom-nav.tsx existe e exporta `BottomNav`
    - components/layout/bottom-nav.tsx contém `'use client'`
    - components/layout/bottom-nav.tsx contém `min-w-[44px] min-h-[44px]` (touch target 44px)
    - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>

  <done>App Shell completo: Header mobile, Sidebar desktop (240px), BottomNav mobile (64px), 4 nav items com Perfil ativo e demais desabilitados com tooltip "Em breve".</done>
</task>

<task type="auto">
  <name>Task 3: Tela de Perfil read-only + componente LogoutButton com AlertDialog</name>
  <files>app/(app)/profile/page.tsx, components/auth/logout-button.tsx</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-UI-SPEC.md (seção "Tela de Perfil — Especificação Visual" e "Copywriting Contract" — Avatar 80px, Heading, email muted, Separator, botão destructive)
    - .planning/phases/01-infrastructure-auth/01-CONTEXT.md (D-02: nome e avatar read-only de auth.users metadata; D-03: somente visualização, sem formulário de edição)
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (seção "Leitura de Dados do Usuário" e "Logout com AlertDialog" — código de referência; Pitfall 7: force-dynamic)
  </read_first>

  <action>
**1. Criar `components/auth/logout-button.tsx`** — Client Component com AlertDialog:

```typescript
'use client'

import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from '@/components/ui/alert-dialog'
import { Button } from '@/components/ui/button'

export function LogoutButton() {
  const router = useRouter()

  async function handleLogout() {
    const supabase = createClient()
    await supabase.auth.signOut()
    router.push('/login')
  }

  return (
    <AlertDialog>
      <AlertDialogTrigger asChild>
        <Button
          variant="destructive"
          size="sm"
          className="w-full"
        >
          Sair da conta
        </Button>
      </AlertDialogTrigger>

      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Sair da conta?</AlertDialogTitle>
          <AlertDialogDescription>
            Você precisará entrar com Google novamente para acessar suas finanças.
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogCancel>Cancelar</AlertDialogCancel>
          <AlertDialogAction onClick={handleLogout}>Sair</AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  )
}
```

**2. Criar `app/(app)/profile/page.tsx`** — Server Component read-only:

```typescript
import Image from 'next/image'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { Separator } from '@/components/ui/separator'
import { LogoutButton } from '@/components/auth/logout-button'

// OBRIGATÓRIO: evitar cache em páginas que leem dados do usuário (Pitfall 7)
export const dynamic = 'force-dynamic'

export default async function ProfilePage() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  // Dados sempre de auth.users metadata (decisão D-02 — read-only do Google)
  const name = (user.user_metadata?.full_name as string) ?? 'Usuário'
  const avatarUrl = user.user_metadata?.avatar_url as string | undefined
  const email = user.email ?? ''

  return (
    <div className="max-w-sm mx-auto">
      {/* Título da página — UI-SPEC: Heading 20px semibold */}
      <h1 className="text-xl font-semibold text-foreground mb-6">Meu Perfil</h1>

      {/* Seção de avatar e dados do usuário */}
      <div className="flex flex-col items-center gap-3 mb-6">
        {/* Avatar 80×80px circular — UI-SPEC */}
        {avatarUrl ? (
          <Image
            src={avatarUrl}
            alt={name}
            width={80}
            height={80}
            className="rounded-full"
            priority
          />
        ) : (
          <div
            className="h-20 w-20 rounded-full bg-muted flex items-center justify-center text-muted-foreground text-xl font-semibold"
            aria-label={name}
          >
            {name.charAt(0).toUpperCase()}
          </div>
        )}

        {/* Nome — UI-SPEC: Heading 20px semibold */}
        <div className="text-center">
          <p className="text-xl font-semibold text-foreground">{name}</p>
          {/* Email — UI-SPEC: Body 14px, muted-foreground */}
          <p className="text-sm text-muted-foreground">{email}</p>
        </div>
      </div>

      {/* Separator — UI-SPEC */}
      <Separator className="mb-6" />

      {/* Logout — UI-SPEC: variant="destructive", size="sm", w=100% */}
      <LogoutButton />
    </div>
  )
}
```

**Checklist de conformidade com UI-SPEC e decisões:**
- [x] D-02: nome e avatar SEMPRE de `user.user_metadata` (não de tabela profiles)
- [x] D-03: somente visualização — sem formulário de edição
- [x] Pitfall 7: `export const dynamic = 'force-dynamic'` presente
- [x] Avatar: `<Image>` Next.js, 80×80px, className="rounded-full" (UI-SPEC)
- [x] Nome: Heading 20px semibold (text-xl font-semibold)
- [x] Email: Body muted (text-sm text-muted-foreground)
- [x] Separator antes do logout (UI-SPEC)
- [x] LogoutButton: variant="destructive", size="sm", w-full (UI-SPEC)
- [x] AlertDialog: copywriting exato do UI-SPEC
  - Title: "Sair da conta?"
  - Body: "Você precisará entrar com Google novamente para acessar suas finanças."
  - Confirm: "Sair"
  - Cancel: "Cancelar"
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10; test -f "app/(app)/profile/page.tsx" && grep -q "force-dynamic" "app/(app)/profile/page.tsx" && grep -q "getUser" "app/(app)/profile/page.tsx" && grep -q "LogoutButton" "app/(app)/profile/page.tsx" && test -f "components/auth/logout-button.tsx" && grep -q "AlertDialog" "components/auth/logout-button.tsx" && echo "OK: profile page e logout button criados"</automated>
  </verify>

  <acceptance_criteria>
    - app/(app)/profile/page.tsx existe
    - app/(app)/profile/page.tsx contém `export const dynamic = 'force-dynamic'` (primeira exportação após imports)
    - app/(app)/profile/page.tsx contém `supabase.auth.getUser()` (não getSession)
    - app/(app)/profile/page.tsx lê `user.user_metadata?.full_name` (não de tabela profiles)
    - app/(app)/profile/page.tsx lê `user.user_metadata?.avatar_url`
    - app/(app)/profile/page.tsx contém `<Image` com width={80} height={80} e className="rounded-full"
    - app/(app)/profile/page.tsx contém `<LogoutButton />` como import do components/auth/logout-button
    - app/(app)/profile/page.tsx NÃO contém formulário de edição (sem <form>, <input>, <textarea>)
    - components/auth/logout-button.tsx existe
    - components/auth/logout-button.tsx contém `'use client'`
    - components/auth/logout-button.tsx contém `supabase.auth.signOut()`
    - components/auth/logout-button.tsx contém texto "Sair da conta?" (título do dialog — copywriting exato)
    - components/auth/logout-button.tsx contém texto "Você precisará entrar com Google novamente para acessar suas finanças." (body exato)
    - components/auth/logout-button.tsx contém `variant="destructive"` no Button trigger
    - `npx tsc --noEmit` passa sem erros em todos os arquivos deste plan
  </acceptance_criteria>

  <done>Tela de perfil read-only com avatar, nome e email do Google. LogoutButton com AlertDialog de confirmação. Copywriting exato do UI-SPEC. `export const dynamic = 'force-dynamic'` presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Supabase Auth | signInWithOAuth e signOut via browser client |
| Server Component → Supabase Auth | getUser() valida token server-side antes de renderizar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Acesso a rota autenticada sem auth | mitigate | middleware.ts (Plan 03) bloqueia antes de chegar ao Server Component; dupla verificação em `app/(app)/layout.tsx` e `profile/page.tsx` com `redirect('/login')` se !user |
| T-04-02 | Information Disclosure | Cache de dados do usuário em página pública | mitigate | `export const dynamic = 'force-dynamic'` em profile/page.tsx — impede cache de dados de usuário (Pitfall 7 do RESEARCH.md) |
| T-04-03 | Tampering | redirectTo manipulado no signInWithOAuth | accept | `redirectTo` aponta para `${window.location.origin}/auth/callback` — origin é controlado pelo browser, não aceita origens cruzadas no PKCE flow do Supabase |
| T-04-04 | Information Disclosure | avatar_url de usuário diferente injetado | accept | avatar_url vem de `user.user_metadata` do token JWT validado pelo servidor Supabase — não é input do usuário; Next.js Image valida domínios no next.config.ts |
</threat_model>

<verification>
Após concluir todas as 3 tasks:

```bash
# TypeScript compila sem erros
npx tsc --noEmit

# Verificar force-dynamic na profile page
grep "force-dynamic" "app/(app)/profile/page.tsx"
# Esperado: export const dynamic = 'force-dynamic'

# Verificar que perfil não tem formulário de edição (decisão D-03)
grep -i "form\|input\|textarea\|edit" "app/(app)/profile/page.tsx" | grep -v "//\|<!--"
# Esperado: sem output

# Verificar copywriting exato do UI-SPEC
grep "Sair da conta?" components/auth/logout-button.tsx
grep "Entrar com Google" "app/(auth)/login/page.tsx"
# Esperado: ambos presentes

# Verificar que app compila e inicializa
npm run build
```
</verification>

<success_criteria>
- `npx tsc --noEmit` passa sem erros em todos os arquivos do plan
- `npm run build` conclui sem erros (pode ter warnings de Image domain config)
- profile/page.tsx tem `export const dynamic = 'force-dynamic'`
- profile/page.tsx não tem formulário de edição (D-03)
- Copywriting idêntico ao UI-SPEC em login e profile
- Acessibilidade: aria-label no botão Google, aria-current na nav, alt no avatar
- Touch targets mínimos: `min-w-[44px] min-h-[44px]` nos itens de nav mobile
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infrastructure-auth/01-04-SUMMARY.md` com:
- Telas implementadas e rotas disponíveis
- Qualquer ajuste de layout vs UI-SPEC (documentar desvios)
- Configuração de next.config.ts para domínios de image (avatars Google)
- Status do `npm run build`
</output>
