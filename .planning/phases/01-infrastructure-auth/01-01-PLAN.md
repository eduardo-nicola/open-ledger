---
phase: 01-infrastructure-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - app/layout.tsx
  - app/globals.css
  - app/page.tsx
  - components.json
  - components/ui/button.tsx
  - components/ui/avatar.tsx
  - components/ui/alert-dialog.tsx
  - components/ui/separator.tsx
  - components/ui/skeleton.tsx
  - components/ui/sheet.tsx
  - components/ui/tooltip.tsx
  - lib/utils.ts
  - lib/providers.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "package.json existe com next, @supabase/ssr, @tanstack/react-query, shadcn/ui, zod como dependências"
    - "tsconfig.json tem strict: true e alias @/* configurado"
    - "shadcn/ui inicializado (components.json presente com baseColor zinc)"
    - "Root layout usa dark mode, font Inter, e envolve children com QueryClientProvider"
    - "Estrutura de diretórios criada (components/layout/, components/auth/, hooks/, types/, lib/supabase/)"
  artifacts:
    - path: "package.json"
      provides: "Dependências do projeto com versões exatas"
      contains: "@supabase/ssr"
    - path: "tsconfig.json"
      provides: "Configuração TypeScript com strict mode"
      contains: "\"strict\": true"
    - path: "components.json"
      provides: "Configuração shadcn/ui"
      contains: "zinc"
    - path: "lib/providers.tsx"
      provides: "QueryClientProvider wrapper para toda a aplicação"
      exports: ["Providers"]
    - path: "app/layout.tsx"
      provides: "Root layout com dark mode, Inter font e Providers"
      contains: "Providers"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/providers.tsx"
      via: "import { Providers } from '@/lib/providers'"
      pattern: "Providers"
    - from: "components/ui/*"
      to: "components.json"
      via: "shadcn registry"
      pattern: "components/ui"
---

<objective>
Criar o projeto Next.js 16 com todas as dependências do stack, inicializar shadcn/ui no tema zinc/dark e configurar a estrutura de diretórios base que todas as fases seguintes vão seguir.

Purpose: Fase 1 começa do zero — repositório completamente vazio. Este plano cria toda a fundação: projeto Next.js, dependências, tema visual e estrutura de pastas.
Output: Projeto Next.js 16 compilando, shadcn/ui inicializado (zinc/dark), QueryClientProvider configurado, estrutura de dirs criada.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
@.planning/phases/01-infrastructure-auth/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar projeto Next.js 16 e instalar todas as dependências do stack</name>
  <files>package.json, tsconfig.json, next.config.ts, app/layout.tsx, app/globals.css, app/page.tsx, .gitignore</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (seção "Standard Stack" e "Installation" — versões exatas das bibliotecas)
    - .planning/phases/01-infrastructure-auth/01-CONTEXT.md (decisões D-09 a D-12 sobre ambiente de desenvolvimento)
  </read_first>

  <action>
O diretório já tem .planning/, .cursor/ e .git/ — o repositório está vazio de código. Executar em ordem:

**1. Criar projeto Next.js 16:**
```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
```
Se perguntar sobre diretório não-vazio, confirmar com Y. Quando perguntar sobre o App Router, selecionar Yes.

**2. Instalar dependências de produção (executar após create-next-app):**
```bash
npm install @supabase/ssr@0.10.2 @supabase/supabase-js@2.103.2
npm install @tanstack/react-query@5.99.0 zustand@5.0.12
npm install react-hook-form@7.72.1 zod @hookform/resolvers
```

Nota sobre zod: instalar `zod` (latest = v4). Se `@hookform/resolvers` falhar com Zod v4, executar `npm install zod@3` para downgrade.

**3. Instalar dependências de desenvolvimento:**
```bash
npm install -D @tanstack/react-query-devtools
npm install -D @playwright/test
```

**4. Atualizar tsconfig.json** para garantir `strict: true`. O create-next-app já cria com strict, mas verificar e garantir:
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "module": "esnext",
    "moduleDetection": "force",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true,
    "jsx": "preserve",
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**5. Atualizar .gitignore** para adicionar entries importantes (após o gerado pelo create-next-app):
```
# Environment
.env.local
.env.*.local

# Supabase
!supabase/.branches
supabase/.branches/*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
```
  </action>

  <verify>
    <automated>node -e "const p = require('./package.json'); ['next','@supabase/ssr','@tanstack/react-query','react-hook-form','zod'].forEach(d => { if(!p.dependencies[d]) throw new Error('Missing: '+d); }); console.log('OK: all deps present')"</automated>
  </verify>

  <acceptance_criteria>
    - package.json contém `"next"` na seção dependencies
    - package.json contém `"@supabase/ssr"` na seção dependencies
    - package.json contém `"@tanstack/react-query"` na seção dependencies
    - package.json contém `"@playwright/test"` na seção devDependencies
    - tsconfig.json contém `"strict": true`
    - tsconfig.json contém `"@/*": ["./*"]` em compilerOptions.paths
    - .gitignore contém `.env.local`
  </acceptance_criteria>

  <done>Projeto Next.js criado com todas as dependências instaladas. `npm list next @supabase/ssr @tanstack/react-query` retorna versões sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Inicializar shadcn/ui (zinc/dark) + criar estrutura de diretórios + configurar providers</name>
  <files>components.json, app/globals.css, app/layout.tsx, app/page.tsx, lib/utils.ts, lib/providers.tsx, components/ui/button.tsx, components/ui/avatar.tsx, components/ui/alert-dialog.tsx, components/ui/separator.tsx, components/ui/skeleton.tsx, components/ui/sheet.tsx, components/ui/tooltip.tsx</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-UI-SPEC.md (seções "Design System", "Color" e "Typography" — tema zinc/dark e tokens de cor)
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (Pitfall 6: shadcn/ui v4 requer Tailwind v4 — configuração diferente)
    - app/layout.tsx (arquivo criado pelo create-next-app — ver estado atual antes de modificar)
    - app/globals.css (arquivo criado pelo create-next-app — ver estado atual antes de modificar)
  </read_first>

  <action>
**1. Inicializar shadcn/ui com preset zinc:**
```bash
npx shadcn@latest init --style default --base-color zinc --css-variables
```
Se não aceitar flags, rodar interativamente e selecionar: style=Default, base color=Zinc, CSS variables=Yes.

Se o comando falhar por causa do Tailwind v4, criar `components.json` manualmente com o conteúdo:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "zinc",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

**2. Adicionar componentes shadcn necessários na Fase 1:**
```bash
npx shadcn@latest add button avatar alert-dialog separator skeleton sheet tooltip
```

**3. Criar estrutura de diretórios** (criar arquivos .gitkeep para preservar nos commits, ou criar diretórios criando arquivos reais):
```
components/
  layout/    ← App Shell, Header, Sidebar, BottomNav (criados no Plan 04)
  auth/      ← LogoutButton (criado no Plan 04)
hooks/       ← Custom hooks (criados nas fases futuras)
types/       ← Tipos TypeScript compartilhados
lib/
  supabase/  ← server.ts e client.ts (criados no Plan 03)
```
Criar um `hooks/.gitkeep` e `types/.gitkeep` para preservar os diretórios vazios.

**4. Criar `lib/providers.tsx`** com QueryClientProvider:
```typescript
'use client'

import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'
import { useState } from 'react'

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            staleTime: 60 * 1000,
          },
        },
      })
  )

  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  )
}
```

**5. Atualizar `app/layout.tsx`** com dark mode, Inter font e Providers:
```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/lib/providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Open Ledger',
  description: 'Suas finanças em um só lugar',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR" className="dark">
      <body className={`${inter.className} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
```

**6. Substituir `app/page.tsx`** com redirect simples (o middleware protegerá a rota):
```typescript
import { redirect } from 'next/navigation'

export default function Home() {
  redirect('/profile')
}
```

**7. Verificar `app/globals.css`**: O `npx shadcn@latest init` deve ter adicionado as CSS variables do zinc/dark. Verificar que o arquivo contém as variáveis `--background`, `--foreground`, `--card`, `--primary` com valores do tema zinc escuro. Se estiver vazio ou com apenas `@tailwind`, significa que o shadcn não configurou — nesse caso, adicionar manualmente o bloco de variáveis zinc dark gerado por `ui.shadcn.com/themes`.
  </action>

  <verify>
    <automated>node -e "const fs = require('fs'); const cj = JSON.parse(fs.readFileSync('components.json','utf8')); if(cj.tailwind.baseColor !== 'zinc') throw new Error('Wrong baseColor: '+cj.tailwind.baseColor); if(!fs.existsSync('components/ui/button.tsx')) throw new Error('Missing button.tsx'); if(!fs.existsSync('lib/providers.tsx')) throw new Error('Missing providers.tsx'); console.log('OK: shadcn zinc configurado, providers existe')"</automated>
  </verify>

  <acceptance_criteria>
    - components.json existe e contém `"baseColor": "zinc"`
    - components.json contém `"rsc": true`
    - components/ui/button.tsx existe
    - components/ui/avatar.tsx existe
    - components/ui/alert-dialog.tsx existe
    - lib/providers.tsx contém `QueryClientProvider`
    - lib/providers.tsx contém `QueryClient`
    - app/layout.tsx contém `className="dark"` na tag html
    - app/layout.tsx contém `Providers` como wrapper
    - app/layout.tsx contém `Inter` de `next/font/google`
    - app/page.tsx contém `redirect('/profile')`
    - app/globals.css contém `--background` (variáveis CSS do shadcn)
  </acceptance_criteria>

  <done>shadcn/ui inicializado com tema zinc/dark. Todos os 7 componentes adicionados. Root layout com dark mode, Inter e QueryClientProvider. `npx tsc --noEmit` passa sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → git | Secrets não devem ser commitados — .env.local deve estar no .gitignore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local / package.json | mitigate | .gitignore contém `.env.local` e `.env.*.local`; .env.example sem valores reais; verificar com `grep -r "NEXT_PUBLIC_SUPABASE" .env.local --include="*.json"` que não há secrets em package.json |
| T-01-02 | Information Disclosure | NEXT_PUBLIC_* vars | accept | NEXT_PUBLIC_ vars são expostas ao client por design — apenas anon key (não service role) é permitida como NEXT_PUBLIC_ |
</threat_model>

<verification>
Após concluir ambas as tasks:

```bash
# TypeScript compila sem erros
npx tsc --noEmit

# shadcn configurado corretamente
cat components.json | grep baseColor   # deve mostrar "zinc"

# Todas as dependências instaladas
npm list next @supabase/ssr @tanstack/react-query react-hook-form zod 2>&1 | grep -v WARN
```
</verification>

<success_criteria>
- `npx tsc --noEmit` passa sem erros TypeScript
- `npm run lint` passa sem erros críticos
- `components.json` existe com baseColor zinc e rsc: true
- `lib/providers.tsx` existe com QueryClientProvider e ReactQueryDevtools
- `app/layout.tsx` usa dark mode class, Inter font e Providers wrapper
- Todos 7 componentes shadcn presentes em `components/ui/`
- Estrutura de diretórios: `components/layout/`, `components/auth/`, `hooks/`, `types/`, `lib/supabase/`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infrastructure-auth/01-01-SUMMARY.md` com:
- Versões exatas instaladas (next, @supabase/ssr, @tanstack/react-query, zod)
- Preset shadcn usado (zinc/dark)
- Qualquer desvio da versão esperada (ex: zod v3 vs v4, Tailwind config)
- Comandos que funcionaram / falharam durante setup
</output>
