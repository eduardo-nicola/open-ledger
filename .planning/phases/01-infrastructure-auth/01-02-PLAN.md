---
phase: 01-infrastructure-auth
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260416000000_initial_schema.sql
  - .env.example
  - .env.local
autonomous: true
requirements:
  - AUTH-03

user_setup:
  - service: google-cloud-console
    why: "Credenciais Google OAuth 2.0 para autenticação"
    dashboard_config:
      - task: "Criar projeto no Google Cloud Console (ou usar existente)"
        location: "console.cloud.google.com → New Project → nome: open-ledger"
      - task: "Habilitar Google+ API / People API"
        location: "APIs & Services → Library → procurar 'Google+ API' → Enable"
      - task: "Criar OAuth 2.0 Client ID"
        location: "APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID → Application type: Web application"
      - task: "Adicionar Authorized redirect URI para dev local"
        location: "OAuth 2.0 Client → Authorized redirect URIs → adicionar: http://localhost:54321/auth/v1/callback"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → Credentials → OAuth 2.0 Client IDs → Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console → Credentials → OAuth 2.0 Client IDs → Client Secret"
  - service: supabase-local
    why: "Banco de dados PostgreSQL local + Auth + Studio"
    dashboard_config:
      - task: "Configurar Google OAuth provider no Supabase config.toml com GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET"
        location: "supabase/config.toml → [auth.external.google] — executor configura automaticamente"
      - task: "Rodar supabase start (abre Docker containers)"
        location: "Terminal → supabase start → aguardar ~30s"
      - task: "Copiar valores de output do supabase start para .env.local"
        location: "Output do terminal → API URL e anon key → colar em .env.local"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Output de `supabase start` → linha 'API URL: http://localhost:54321'"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Output de `supabase start` → linha 'anon key: eyJ...'"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Output de `supabase start` → linha 'service_role key: eyJ...'"

must_haves:
  truths:
    - "Supabase CLI instalado e `supabase init` executado (supabase/config.toml existe)"
    - "Migration SQL existe com tabelas profiles, accounts e transactions"
    - "RLS habilitado e 4 policies criadas para cada uma das 3 tabelas (12 policies total)"
    - "Trigger `on_auth_user_created` existe com função SECURITY DEFINER"
    - ".env.example documenta as 3 variáveis sem valores reais"
    - "Dois usuários distintos não conseguem acessar dados um do outro via RLS"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Configuração Supabase local com Google OAuth"
      contains: "[auth.external.google]"
    - path: "supabase/migrations/20260416000000_initial_schema.sql"
      provides: "Schema base: profiles + accounts + transactions + RLS + trigger"
      contains: "CREATE TABLE public.profiles"
    - path: ".env.example"
      provides: "Documentação de variáveis de ambiente"
      contains: "NEXT_PUBLIC_SUPABASE_URL="
  key_links:
    - from: "supabase/migrations/20260416000000_initial_schema.sql"
      to: "public.profiles"
      via: "RLS policy: auth.uid() = id"
      pattern: "auth.uid\\(\\) = id"
    - from: "on_auth_user_created trigger"
      to: "public.profiles"
      via: "INSERT INTO public.profiles (id) VALUES (NEW.id)"
      pattern: "INSERT INTO public.profiles"
---

<objective>
Configurar o Supabase localmente via CLI + Docker, criar o schema inicial do banco de dados com as três tabelas core (profiles, accounts, transactions), habilitar RLS com 4 policies em cada tabela, e criar o trigger automático de criação de profiles.

Purpose: O isolamento de dados (AUTH-03) depende de RLS funcionando corretamente. Um erro aqui — como tabela sem policies ou trigger sem SECURITY DEFINER — causa bugs silenciosos onde queries retornam 0 rows.
Output: Supabase CLI instalado, `supabase/` diretório configurado, migration SQL completa pronta para `supabase db push`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-infrastructure-auth/01-CONTEXT.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
</context>

<interfaces>
<!-- Schema base que as fases seguintes vão expandir incrementalmente. -->
<!-- Campos específicos (ex: installment_group_id, credit_limit) são adicionados por migrations futuras. -->

Schema das tabelas:
```sql
-- public.profiles: âncora de RLS (apenas id + timestamps)
-- user_id em todas as outras tabelas faz FK para public.profiles(id), NÃO para auth.users diretamente

-- public.accounts:
--   id UUID PK, user_id UUID FK→profiles, name TEXT, type TEXT CHECK(...), 
--   color TEXT, balance INTEGER (centavos), currency TEXT, 
--   created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ, archived_at TIMESTAMPTZ NULL

-- public.transactions:
--   id UUID PK, user_id UUID FK→profiles, account_id UUID FK→accounts,
--   amount INTEGER (centavos signed), date DATE (não TIMESTAMPTZ),
--   description TEXT, type TEXT CHECK('expense'|'income'),
--   status TEXT CHECK('paid'|'pending'),
--   created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Supabase CLI + supabase init + configurar config.toml + criar .env.example</name>
  <files>supabase/config.toml, .env.example, .env.local</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-CONTEXT.md (decisões D-09 a D-12: Supabase CLI local, .env.example, supabase/ dir commitado)
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (seção "Configuração do Supabase Local" e "Pitfall 8: Callback URL Não Configurada")
  </read_first>

  <action>
**1. Instalar Supabase CLI globalmente:**
```bash
npm install -g supabase
```
Verificar instalação: `supabase --version` deve retornar versão >= 1.x.

**2. Inicializar Supabase no projeto:**
```bash
supabase init
```
Isso cria o diretório `supabase/` com `config.toml` e a estrutura de migrations.

**3. Editar `supabase/config.toml`** para configurar Google OAuth e a URL do site:

Localizar as seções existentes no arquivo gerado e atualizar/adicionar:

```toml
[auth]
enabled = true
site_url = "http://127.0.0.1:3000"
additional_redirect_urls = ["http://localhost:3000/**", "http://127.0.0.1:3000/**"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true
enable_anonymous_sign_ins = false

[auth.email]
enable_signup = false
double_confirm_changes = true
enable_confirmations = false

[auth.external.google]
enabled = true
client_id = "env(GOOGLE_CLIENT_ID)"
secret = "env(GOOGLE_CLIENT_SECRET)"
redirect_uri = ""
url = ""
skip_nonce_check = false
```

**4. Criar `.env.example`** na raiz do projeto (SEM valores reais):
```bash
# Supabase — obter via `supabase start`
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth — obter no Google Cloud Console (console.cloud.google.com)
# Redirect URI para dev local: http://localhost:54321/auth/v1/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
```

**5. Criar `.env.local`** como template para o usuário preencher:
```bash
# Supabase — rodar `supabase start` e copiar os valores do output
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=SUBSTITUIR_PELO_ANON_KEY_DO_SUPABASE_START
SUPABASE_SERVICE_ROLE_KEY=SUBSTITUIR_PELO_SERVICE_ROLE_KEY_DO_SUPABASE_START

# Google OAuth — configurar no Google Cloud Console
GOOGLE_CLIENT_ID=SUBSTITUIR_PELO_CLIENT_ID_DO_GOOGLE
GOOGLE_CLIENT_SECRET=SUBSTITUIR_PELO_CLIENT_SECRET_DO_GOOGLE
```

**IMPORTANTE:** `.env.local` contém placeholders, não valores reais. O usuário preenche após rodar `supabase start`.

**6. Verificar `.gitignore`** — garantir que `.env.local` está no .gitignore (foi adicionado no Plan 01 Task 1, mas confirmar).
  </action>

  <verify>
    <automated>test -f supabase/config.toml && grep -q "auth.external.google" supabase/config.toml && test -f .env.example && grep -q "NEXT_PUBLIC_SUPABASE_URL=" .env.example && echo "OK: supabase init + config.toml + .env.example OK"</automated>
  </verify>

  <acceptance_criteria>
    - supabase/config.toml existe
    - supabase/config.toml contém `[auth.external.google]`
    - supabase/config.toml contém `client_id = "env(GOOGLE_CLIENT_ID)"`
    - supabase/config.toml contém `site_url = "http://127.0.0.1:3000"`
    - .env.example existe
    - .env.example contém `NEXT_PUBLIC_SUPABASE_URL=` (sem valor)
    - .env.example contém `NEXT_PUBLIC_SUPABASE_ANON_KEY=` (sem valor)
    - .env.example contém `SUPABASE_SERVICE_ROLE_KEY=` (sem valor)
    - .env.example contém `GOOGLE_CLIENT_ID=` (sem valor)
    - .env.local existe
    - .gitignore contém `.env.local`
    - supabase CLI instalado: `supabase --version` retorna sem erro
  </acceptance_criteria>

  <done>Supabase CLI instalado, diretório supabase/ criado, config.toml com Google OAuth configurado, .env.example documentando as 5 variáveis sem valores reais.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar migration SQL completa — profiles + accounts + transactions + RLS + trigger</name>
  <files>supabase/migrations/20260416000000_initial_schema.sql</files>

  <read_first>
    - .planning/phases/01-infrastructure-auth/01-CONTEXT.md (D-01: profiles minimalista; D-05: tabelas core; D-08: INTEGER centavos, DATE, RLS 4 policies; D-04: trigger SECURITY DEFINER)
    - .planning/phases/01-infrastructure-auth/01-RESEARCH.md (Pattern 4: Trigger para public.profiles; Pattern 5: Schema base de accounts e transactions; Pitfall 3: RLS sem policy; Pitfall 5: Trigger sem SECURITY DEFINER)
  </read_first>

  <action>
Criar o arquivo `supabase/migrations/20260416000000_initial_schema.sql` com o seguinte conteúdo COMPLETO (copiar exatamente):

```sql
-- =============================================================================
-- Open Ledger — Schema Inicial (Fase 1)
-- Migration: 20260416000000_initial_schema.sql
--
-- Cria as tabelas core do banco. Fases futuras adicionam campos e tabelas
-- incrementalmente via novas migrations.
--
-- Decisões de schema (STATE.md):
--   - Valores monetários: INTEGER (centavos) — nunca FLOAT/NUMERIC
--   - Datas de transação: DATE — nunca TIMESTAMPTZ (evita timezone corruption)
--   - RLS: 4 policies por tabela (SELECT, INSERT, UPDATE, DELETE)
--   - FK chain: auth.users → profiles → accounts/transactions
-- =============================================================================

-- ---------------------------------------------------------------------------
-- Tabela: public.profiles
-- Âncora de RLS. Contém apenas id (FK para auth.users) + timestamps.
-- Nome, email e avatar são sempre lidos de auth.users (metadata Google OAuth).
-- Decisão D-01: sem campos editáveis pelo usuário.
-- ---------------------------------------------------------------------------
CREATE TABLE public.profiles (
  id          UUID        PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "profiles: users can select own row"
  ON public.profiles FOR SELECT
  USING (auth.uid() = id);

CREATE POLICY "profiles: users can insert own row"
  ON public.profiles FOR INSERT
  WITH CHECK (auth.uid() = id);

CREATE POLICY "profiles: users can update own row"
  ON public.profiles FOR UPDATE
  USING (auth.uid() = id)
  WITH CHECK (auth.uid() = id);

CREATE POLICY "profiles: users can delete own row"
  ON public.profiles FOR DELETE
  USING (auth.uid() = id);

-- ---------------------------------------------------------------------------
-- Trigger: criação automática de profiles no primeiro login
-- Decisão D-04: trigger on auth.users INSERT com SECURITY DEFINER.
-- SECURITY DEFINER é obrigatório para a função ter permissão de inserir em
-- public.profiles durante o INSERT em auth.users (Pitfall 5 do RESEARCH.md).
-- ---------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id)
  VALUES (NEW.id)
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = public;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- ---------------------------------------------------------------------------
-- Tabela: public.accounts
-- Schema base — campos específicos de parcelamento/fatura são adicionados
-- em fases futuras via migrations separadas (decisão D-06).
-- ---------------------------------------------------------------------------
CREATE TABLE public.accounts (
  id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID        NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  name        TEXT        NOT NULL,
  type        TEXT        NOT NULL CHECK (type IN ('checking', 'savings', 'digital_wallet', 'credit_card')),
  color       TEXT        NOT NULL DEFAULT '#22c55e',
  balance     INTEGER     NOT NULL DEFAULT 0,       -- centavos (decisão D-08)
  currency    TEXT        NOT NULL DEFAULT 'BRL',
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  archived_at TIMESTAMPTZ
);

ALTER TABLE public.accounts ENABLE ROW LEVEL SECURITY;

CREATE POLICY "accounts: users can select own rows"
  ON public.accounts FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "accounts: users can insert own rows"
  ON public.accounts FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "accounts: users can update own rows"
  ON public.accounts FOR UPDATE
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "accounts: users can delete own rows"
  ON public.accounts FOR DELETE
  USING (auth.uid() = user_id);

-- ---------------------------------------------------------------------------
-- Tabela: public.transactions
-- Schema base — campos de parcelamento (installment_group_id) e de fatura
-- (invoice_id) são adicionados nas Fases 4 e 5 via migrations separadas.
-- ---------------------------------------------------------------------------
CREATE TABLE public.transactions (
  id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID        NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  account_id  UUID        NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  amount      INTEGER     NOT NULL,                 -- centavos, signed (negativo=despesa)
  date        DATE        NOT NULL,                 -- DATE, não TIMESTAMPTZ (decisão D-08)
  description TEXT        NOT NULL DEFAULT '',
  type        TEXT        NOT NULL CHECK (type IN ('expense', 'income')),
  status      TEXT        NOT NULL DEFAULT 'pending' CHECK (status IN ('paid', 'pending')),
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE public.transactions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "transactions: users can select own rows"
  ON public.transactions FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "transactions: users can insert own rows"
  ON public.transactions FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "transactions: users can update own rows"
  ON public.transactions FOR UPDATE
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "transactions: users can delete own rows"
  ON public.transactions FOR DELETE
  USING (auth.uid() = user_id);

-- ---------------------------------------------------------------------------
-- Índices de performance para queries por data (Pitfall 20 do RESEARCH.md)
-- ---------------------------------------------------------------------------
CREATE INDEX idx_transactions_account_date
  ON public.transactions (account_id, date DESC);

CREATE INDEX idx_transactions_user_date
  ON public.transactions (user_id, date DESC);
```

**IMPORTANTE:** Não rodar `supabase db push` agora — isso é parte do `user_setup`. O executor apenas cria o arquivo SQL. O usuário roda `supabase start` + `supabase db push` após configurar as credenciais Google no .env.local.
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE public\." supabase/migrations/20260416000000_initial_schema.sql | xargs -I{} test {} -eq 3 && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260416000000_initial_schema.sql | xargs -I{} test {} -eq 3 && grep -c "CREATE POLICY" supabase/migrations/20260416000000_initial_schema.sql | xargs -I{} test {} -ge 12 && grep -q "SECURITY DEFINER" supabase/migrations/20260416000000_initial_schema.sql && echo "OK: migration SQL válida — 3 tabelas, 3 RLS, 12+ policies, SECURITY DEFINER"</automated>
  </verify>

  <acceptance_criteria>
    - supabase/migrations/20260416000000_initial_schema.sql existe
    - Arquivo contém `CREATE TABLE public.profiles`
    - Arquivo contém `CREATE TABLE public.accounts`
    - Arquivo contém `CREATE TABLE public.transactions`
    - Arquivo contém exatamente 3 ocorrências de `ENABLE ROW LEVEL SECURITY`
    - Arquivo contém pelo menos 12 ocorrências de `CREATE POLICY` (4 por tabela)
    - Arquivo contém `SECURITY DEFINER` na função handle_new_user
    - Arquivo contém `SET search_path = public` na função (segurança: evita search_path injection)
    - Arquivo contém `ON CONFLICT (id) DO NOTHING` no trigger (idempotente)
    - Arquivo contém `amount INTEGER` (não NUMERIC, não FLOAT)
    - Arquivo contém `date DATE` (não TIMESTAMPTZ)
    - Arquivo contém índices: `idx_transactions_account_date` e `idx_transactions_user_date`
    - accounts.type CHECK contém `'checking', 'savings', 'digital_wallet', 'credit_card'`
    - transactions.type CHECK contém `'expense', 'income'`
    - transactions.status CHECK contém `'paid', 'pending'`
  </acceptance_criteria>

  <done>Migration SQL criada com schema completo. 3 tabelas, 12 RLS policies, trigger com SECURITY DEFINER, índices de performance. Arquivo pronto para `supabase db push` após `supabase start`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.users → public.profiles | Trigger cruza a boundary — SECURITY DEFINER necessário |
| client app → Postgres via RLS | Cada query filtrada pelo auth.uid() do JWT |
| .env.local → git | Service role key não deve ser commitada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | RLS em todas as tabelas | mitigate | 4 policies criadas no mesmo migration que cria a tabela — nunca habilitar RLS sem policies (Pitfall 3); verificar com `grep -c "CREATE POLICY"` = 12 |
| T-02-02 | Elevation of Privilege | Função handle_new_user | mitigate | `SECURITY DEFINER SET search_path = public` — sem isso, trigger falha silenciosamente (Pitfall 5) ou é vulnerável a search_path injection |
| T-02-03 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Variável sem prefixo NEXT_PUBLIC_ no .env.example; .env.local no .gitignore; service role key bypassa RLS completamente |
| T-02-04 | Tampering | SQL injection via Supabase client | accept | Supabase client usa queries parametrizadas automaticamente — interpolação de string em queries raw está fora de escopo desta fase |
| T-02-05 | Elevation of Privilege | Cross-user data access | mitigate | RLS com `auth.uid() = user_id` em todas as 3 tabelas garante isolamento total; testar com dois usuários distintos no Plan 05 |
</threat_model>

<verification>
Após concluir ambas as tasks:

```bash
# Verificar estrutura da migration
grep -c "CREATE TABLE public\." supabase/migrations/20260416000000_initial_schema.sql
# Esperado: 3

grep -c "CREATE POLICY" supabase/migrations/20260416000000_initial_schema.sql
# Esperado: 12

grep "SECURITY DEFINER" supabase/migrations/20260416000000_initial_schema.sql
# Esperado: linha com handle_new_user

# Verificar .env.example não tem valores
grep "=" .env.example | grep -v "=$"
# Esperado: sem output (todas as vars sem valor)
```

**User setup necessário antes da verificação completa:**
1. Configurar Google Cloud Console (criar OAuth credentials)
2. Preencher GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET em .env.local
3. Rodar `supabase start` e copiar NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY para .env.local
4. Rodar `supabase db push` para aplicar as migrations
</verification>

<success_criteria>
- `supabase/migrations/20260416000000_initial_schema.sql` existe com conteúdo completo
- 3 tabelas criadas, cada uma com ENABLE ROW LEVEL SECURITY
- 12 CREATE POLICY statements (4 por tabela)
- Trigger `on_auth_user_created` com função SECURITY DEFINER
- `.env.example` com 5 variáveis documentadas sem valores
- `.env.local` com placeholders para o usuário preencher
- `supabase/config.toml` com [auth.external.google] configurado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-infrastructure-auth/01-02-SUMMARY.md` com:
- Estrutura do schema criado (tabelas e campos principais)
- Políticas RLS implementadas
- Qualquer desvio do schema planejado
- Instruções para o usuário completar o user_setup (supabase start + credenciais Google)
</output>
