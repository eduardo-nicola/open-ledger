---
phase: 02-account-management
plan: 3
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - app/(app)/accounts/page.tsx
  - app/(app)/accounts/new/page.tsx
  - app/(app)/accounts/[id]/page.tsx
  - app/(app)/accounts/[id]/edit/page.tsx
  - components/accounts/account-list.tsx
  - components/accounts/account-form.tsx
  - components/accounts/balance-chart.tsx
  - components/accounts/archive-account-button.tsx
  - components/layout/bottom-nav.tsx
  - components/layout/sidebar.tsx
  - package.json
  - package-lock.json
  - components/ui/chart.tsx
  - app/globals.css
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05
must_haves:
  truths:
    - "Rotas /accounts, /accounts/new, /accounts/[id], /accounts/[id]/edit renderizam com force-dynamic"
    - "Nav Contas habilitada para /accounts"
    - "Gráfico de linha 30d na página de detalhe usando Recharts + shadcn chart"
  artifacts:
    - path: app/(app)/accounts/page.tsx
      provides: "Lista + consolidado"
      contains: "getConsolidatedBalanceCents"
    - path: components/accounts/balance-chart.tsx
      provides: "ACC-05 visual"
      contains: "LineChart|Line"
  key_links:
    - from: "app/(app)/accounts/page.tsx"
      to: "lib/accounts/queries.ts"
      via: "import getConsolidatedBalanceCents"
      pattern: "getConsolidatedBalanceCents"
---

<objective>
Entregar as telas de gestão de contas conforme **02-UI-SPEC.md** e **01-UI-SPEC.md** (tokens herdados): lista com consolidado, fluxos de criação/edição com três tipos e dias do cartão, detalhe com gráfico ACC-05, navegação ativa.

Propósito: cumprir critérios de aceite visíveis ao usuário e D-06/D-12/D-13/D-14.

Saída: rotas em `app/(app)/accounts/**`, componentes em `components/accounts/`, dependência `recharts` + componente `chart` shadcn.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-auth/01-UI-SPEC.md
@.planning/phases/02-account-management/02-UI-SPEC.md
@app/(app)/layout.tsx
@app/(app)/profile/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Dependências Recharts + shadcn chart</name>
  <files>package.json, package-lock.json, components/ui/chart.tsx, app/globals.css</files>
  <read_first>
    - package.json
    - .planning/phases/02-account-management/02-RESEARCH.md (Installation incremental)
    - components.json (se existir na raiz; senão criar via shadcn init conforme repo)
  </read_first>
  <action>
    Executar `npm install recharts@^3.8.1` e `npx shadcn@latest add chart --yes` (ou equivalente documentado no projeto) gerando `components/ui/chart.tsx`. Mesclar tokens `--chart-1` … no tema: se `add chart` alterar `app/globals.css`, manter coerência com zinc/dark da Fase 1.
  </action>
  <acceptance_criteria>
    - `rg '"recharts"' package.json` retorna match.
    - Arquivo `components/ui/chart.tsx` existe.
    - `npm run lint` exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f components/ui/chart.tsx && rg -n "recharts" package.json && npm run lint</automated>
  </verify>
  <done>recharts instalado e chart shadcn presente; lint OK.</done>
</task>

<task type="auto">
  <name>Páginas lista, nova, detalhe e editar</name>
  <files>app/(app)/accounts/page.tsx, app/(app)/accounts/new/page.tsx, app/(app)/accounts/[id]/page.tsx, app/(app)/accounts/[id]/edit/page.tsx, components/accounts/account-list.tsx, components/accounts/account-form.tsx, components/accounts/balance-chart.tsx, components/accounts/archive-account-button.tsx</files>
  <read_first>
    - lib/accounts/queries.ts
    - lib/accounts/actions.ts
    - lib/accounts/balance-history.ts
    - lib/accounts/schema.ts
    - .planning/phases/02-account-management/02-UI-SPEC.md
  </read_first>
  <action>
    Criar rotas (cada arquivo com `export const dynamic = 'force-dynamic'`):
    - `app/(app)/accounts/page.tsx` — `h1` "Contas"; chama `getAccountsForUser()` e `getConsolidatedBalanceCents()`; topo: card consolidado (label tipo "Saldo total" PT-BR) com **atributos** `data-testid="consolidated-balance-cents"` e `data-value="{centavos}"` onde `{centavos}` é o inteiro retornado pela query (string decimal sem formatação); lista unificada ativas+arquivadas: itens com `archived_at` recebem `opacity-60` e `<Badge>Arquivada</Badge>` (D-06); menu ações (DropdownMenu): Editar → link `/accounts/[id]/edit`, Arquivar/Desarquivar via forms POSTando para Server Actions `archiveAccount`/`unarchiveAccount`; link "Nova conta" → `/accounts/new`. Cartões na lista: exibir saldo formatado ou "—" conforme 02-UI-SPEC (escolher "—" para `credit_card` se saldo não for UX útil).
    - `app/(app)/accounts/new/page.tsx` — fluxo ACC-01/ACC-03: escolha de tipo (três cards PT-BR); `AccountForm` client ou server form com RHF+zod resolver importando schemas de `lib/accounts/schema.ts`; cartão exige `closing_day`/`due_day`; submit chama `createAccount`.
    - `app/(app)/accounts/[id]/page.tsx` — detalhe: nome, tipo, saldo; montar `getBalanceHistoryForAccount(id, 30)`; renderizar `BalanceChart` (client) com empty state se **todos** os pontos tiverem `balanceCents === 0` **e** (opcional) nenhuma transação — para simplificar grepável: empty state quando `balanceCents` soma 0 em todos os pontos (equivalente saldo zero); CTA texto contendo substring `Lançar primeira transação` com `href="/transactions"` ou `href="#"` se rota ainda não existir (D-14 stub permitido).
    - `app/(app)/accounts/[id]/edit/page.tsx` — mesmo formulário parcial (nome, cor, dias se cartão); `updateAccount`.
    Extrair UI reutilizável em `components/accounts/account-list.tsx`, `account-form.tsx`, `balance-chart.tsx` (LineChart Recharts dentro de `ChartContainer`), `archive-account-button.tsx` (AlertDialog confirmação recomendado no UI-SPEC).
    Garantir acessibilidade: swatches `role="radiogroup"` conforme 02-UI-SPEC.
  </action>
  <acceptance_criteria>
    - Quatro arquivos existem e cada um contém `export const dynamic = 'force-dynamic'`: `app/(app)/accounts/page.tsx`, `app/(app)/accounts/new/page.tsx`, `app/(app)/accounts/[id]/page.tsx`, `app/(app)/accounts/[id]/edit/page.tsx`.
    - `rg -n "Lançar primeira transação" components/accounts/balance-chart.tsx` retorna ≥1 linha.
    - `rg -n "getBalanceHistoryForAccount" components/accounts/balance-chart.tsx` retorna ≥1 linha.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'for f in "app/(app)/accounts/page.tsx" "app/(app)/accounts/new/page.tsx" "app/(app)/accounts/[id]/page.tsx" "app/(app)/accounts/[id]/edit/page.tsx"; do rg -q "force-dynamic" "$f" || exit 1; done' && rg -n "Lançar primeira transação" components/accounts/balance-chart.tsx && rg -n "getBalanceHistoryForAccount" components/accounts/balance-chart.tsx && rg -n "data-testid=\"consolidated-balance-cents\"" "app/(app)/accounts/page.tsx"</automated>
  </verify>
  <done>Quatro rotas accounts, componentes accounts, data-testid consolidado e gráfico/empty state.</done>
</task>

<task type="auto">
  <name>Habilitar nav Contas (sidebar + bottom)</name>
  <files>components/layout/bottom-nav.tsx, components/layout/sidebar.tsx</files>
  <read_first>
    - components/layout/bottom-nav.tsx
    - components/layout/sidebar.tsx
    - .planning/phases/02-account-management/02-UI-SPEC.md (Navegação)
  </read_first>
  <action>
    Em `navItems` de ambos os arquivos, alterar somente a entrada de **Contas** para `activeInPhase1: true` (mesmo padrão do item Perfil). Não habilitar `/dashboard` nem `/transactions` nesta fase.
  </action>
  <acceptance_criteria>
    - `rg -n "href: '/accounts', label: 'Contas', icon: Landmark, activeInPhase1: true" components/layout/bottom-nav.tsx` retorna 1 linha.
    - `rg -n "href: '/accounts', label: 'Contas', icon: Landmark, activeInPhase1: true" components/layout/sidebar.tsx` retorna 1 linha.
    - `rg -n "href: '/dashboard'.*activeInPhase1: false" components/layout/bottom-nav.tsx` ainda retorna match (dashboard permanece desabilitado).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "href: '/accounts', label: 'Contas', icon: Landmark, activeInPhase1: true" components/layout/bottom-nav.tsx components/layout/sidebar.tsx</automated>
  </verify>
  <done>Item Contas habilitado em bottom-nav e sidebar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Actions | FormData / campos expostos |
| HTML renderizado | XSS em `name` — escapar React default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | formulários conta | mitigate | Zod já aplicado nas actions; max length nome |
| T-02-07 | X | `balance-chart` | mitigate | Dados só vindos de queries servidor; sem `dangerouslySetInnerHTML` |
| T-02-08 | D | nav | accept | Links públicos de rota autenticada |
</threat_model>

<verification>
`npm run build` sem erros após páginas criadas.
</verification>

<success_criteria>
- Quatro rotas funcionais com layout existente `(app)`.
- ACC-04 visível no topo de `/accounts`.
- ACC-05 gráfico ou empty state na página de detalhe.
- Contas acessível pela navegação principal.
</success_criteria>

<must_haves>
- D-14: CTA string exata parcialmente grepável "Lançar primeira transação".
</must_haves>

<output>
Após conclusão: `.planning/phases/02-account-management/02-03-SUMMARY.md`
</output>
