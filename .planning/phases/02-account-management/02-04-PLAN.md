---
phase: 02-account-management
plan: 4
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - tests/e2e/accounts.spec.ts
  - playwright.config.ts
  - tests/e2e/README.md
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05
must_haves:
  truths:
    - "Cada requisito ACC-01..05 tem pelo menos um teste grepável por tag"
    - "Projeto chromium do Playwright executa accounts.spec.ts após auth.setup"
  artifacts:
    - path: tests/e2e/accounts.spec.ts
      provides: "Cobertura E2E contas"
      contains: "@acc-01"
    - path: playwright.config.ts
      provides: "Orquestração"
      contains: "accounts.spec.ts"
  key_links:
    - from: "tests/e2e/accounts.spec.ts"
      to: "storageState"
      via: "dependência projeto setup"
      pattern: "dependencies: ['setup']"
---

<objective>
Adicionar suíte E2E **accounts** com tags `@acc-01` … `@acc-05` (e `@smoke` onde necessário para o comando `npm run test:e2e:smoke`), integrar ao `playwright.config.ts` e documentar pré-requisitos no README dos testes.

Propósito: fechar o loop Nyquist da Fase 2 e rastreabilidade TST-04.

Saída: `tests/e2e/accounts.spec.ts`, config atualizada, README alinhado.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@playwright.config.ts
@tests/e2e/auth.spec.ts
@tests/e2e/README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Criar tests/e2e/accounts.spec.ts</name>
  <files>tests/e2e/accounts.spec.ts</files>
  <read_first>
    - tests/e2e/auth.spec.ts
    - tests/e2e/auth.setup.ts
    - playwright.config.ts
    - .planning/phases/02-account-management/02-VALIDATION.md
  </read_first>
  <action>
    Criar `tests/e2e/accounts.spec.ts` com `test.describe('Accounts', …)` e **cinco** testes, cada um com **título** contendo as tags no texto grepável do Playwright (padrão do projeto para `grep`):
    - `@smoke @acc-01` — fluxo: após storage autenticado, ir a `/accounts/new`, criar **Conta bancária** com nome único (`acc01-${Date.now()}`), cor primeira swatch, submit; esperar URL `/accounts` ou toast sucesso; `expect` linha com nome na lista.
    - `@smoke @acc-02` — criar conta auxiliar ou reusar; abrir `/accounts/[id]/edit` (resolver id via lista); alterar nome suffix `-edited`; salvar; `expect` texto novo; acionar Arquivar (AlertDialog); `expect` badge ou texto "Arquivada" visível.
    - `@smoke @acc-03` — `/accounts/new` → tipo cartão; preencher nome, `closing_day` 10, `due_day` 17`, cor; salvar; opcional: assert via UI mostrando dias (ou documentar que SQL assert fica manual — mínimo: formulário não erro e redireciona).
    - `@smoke @acc-04` — preparar duas contas `checking`/`digital_wallet` com saldos A e B (centavos) via **Supabase service role** em `beforeAll` (documentado no README; usar `process.env.SUPABASE_SERVICE_ROLE_KEY` já padrão E2E se existir) após criar contas pela UI com nomes únicos; assert: `await expect(page.locator('[data-testid="consolidated-balance-cents"]')).toHaveAttribute('data-value', String(A+B))` (valores conhecidos pelo teste).
    - `@smoke @acc-05` — abrir detalhe de conta checking; `expect` SVG do gráfico (`role="img"` ou `class` recharts) **ou** empty state com substring `Lançar primeira transação`.
    Usar `baseURL` implícito do Playwright; timeouts padrão.
  </action>
  <acceptance_criteria>
    - `rg -n "@acc-01|@acc-02|@acc-03|@acc-04|@acc-05" tests/e2e/accounts.spec.ts` retorna ≥5 linhas.
    - `rg -n "@smoke" tests/e2e/accounts.spec.ts` retorna ≥5 linhas.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@acc-01" tests/e2e/accounts.spec.ts && rg -n "@acc-05" tests/e2e/accounts.spec.ts</automated>
  </verify>
  <done>accounts.spec.ts contém 5 testes com @acc-01..05 e @smoke.</done>
</task>

<task type="auto">
  <name>Playwright: incluir accounts.spec no projeto chromium</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts
  </read_first>
  <action>
    No projeto nomeado `chromium`, adicionar `**/accounts.spec.ts` ao array `testMatch` existente (manter `auth.spec.ts` e `rls.spec.ts`). Garantir `dependencies: ['setup']` inalterado.
  </action>
  <acceptance_criteria>
    - `rg -n "accounts.spec.ts" playwright.config.ts` retorna match dentro de `testMatch`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "accounts.spec.ts" playwright.config.ts</automated>
  </verify>
  <done>Projeto chromium inclui accounts.spec.ts em testMatch.</done>
</task>

<task type="auto">
  <name>README tests/e2e: contas e data-testid consolidado</name>
  <files>tests/e2e/README.md</files>
  <read_first>
    - tests/e2e/README.md
    - .planning/phases/02-account-management/02-VALIDATION.md
  </read_first>
  <action>
    Atualizar `tests/e2e/README.md` com subseção **Contas (Fase 2)** listando: comando `npx playwright test tests/e2e/accounts.spec.ts -g @acc-04`; nota que `@smoke` inclui contas após merge; pré-requisito `supabase db push` / `supabase db reset` quando migrations mudarem; variáveis `.env.test` se usadas.
    Documentar `data-testid="consolidated-balance-cents"` como contrato de assert ACC-04.
  </action>
  <acceptance_criteria>
    - `rg -n "consolidated-balance-cents" tests/e2e/README.md` retorna ≥1 linha.
    - `rg -n "accounts.spec.ts" tests/e2e/README.md` retorna ≥1 linha.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "consolidated-balance-cents|accounts.spec" tests/e2e/README.md</automated>
  </verify>
  <done>README documenta contas E2E e data-testid consolidado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E → app local | testes rodam contra dev server; sem credenciais em asserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | I | README | accept | Documentar uso local apenas |
| T-02-10 | T | service role em teste | mitigate | Restringir a `beforeAll` opcional; nunca commitar keys; usar env CI existente |
</threat_model>

<verification>
`npm run test:e2e:smoke` completa com sucesso após implementação dos planos 02-01..03 (pode falhar até UI pronta — ordem de execução: este plano por último).
</verification>

<success_criteria>
- Tags ACC cobertas e smoke verde em ambiente configurado.
- README atualizado para onboarding de testes de contas.
</success_criteria>

<must_haves>
- `data-testid="consolidated-balance-cents"` entregue no plano 02-03; este plano apenas consome no assert ACC-04.
</must_haves>

<output>
Após conclusão: `.planning/phases/02-account-management/02-04-SUMMARY.md`
</output>
