---
phase: 02-account-management
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260423000000_accounts_credit_card_days.sql
  - types/database.ts
autonomous: false
requirements:
  - ACC-03
must_haves:
  truths:
    - "Tabela public.accounts possui colunas closing_day e due_day com CHECK coerente para credit_card"
    - "Migrations aplicadas no Postgres local via supabase db push"
    - "types/database.ts reflete closing_day e due_day nas linhas Row/Insert/Update de accounts"
  artifacts:
    - path: supabase/migrations/20260423000000_accounts_credit_card_days.sql
      provides: "Colunas e constraints ACC-03"
      contains: "closing_day"
    - path: types/database.ts
      provides: "Tipos TS alinhados ao schema"
      contains: "closing_day"
  key_links:
    - from: "migration SQL"
      to: "types/database.ts"
      via: "regeneração manual ou supabase gen types"
      pattern: "closing_day"
---

<objective>
Adicionar ao schema Postgres os campos de cartão de crédito **dia de fechamento** e **dia de vencimento** (ACC-03), alinhar tipos TypeScript e aplicar a migration no ambiente local antes de qualquer código de UI que dependa das colunas.

Propósito: desbloquear validação Zod/Server Actions e queries sem divergência schema/código.

Saída: novo arquivo em `supabase/migrations/`, `types/database.ts` atualizado, banco local atualizado após `supabase db push`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-account-management/02-CONTEXT.md
@.planning/phases/02-account-management/02-RESEARCH.md
@supabase/migrations/20260416000000_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Migration: closing_day e due_day em accounts</name>
  <files>supabase/migrations/20260423000000_accounts_credit_card_days.sql</files>
  <read_first>
    - supabase/migrations/20260416000000_initial_schema.sql
    - .planning/phases/02-account-management/02-CONTEXT.md (D-09, D-10)
    - .planning/phases/02-account-management/02-RESEARCH.md (seção Migration sketch)
  </read_first>
  <action>
    Criar `supabase/migrations/20260423000000_accounts_credit_card_days.sql` com:
    - `ALTER TABLE public.accounts ADD COLUMN closing_day smallint, ADD COLUMN due_day smallint;` (nomes exatos `closing_day`, `due_day`).
    - Constraint nomeada `accounts_credit_card_days_chk` com `CHECK (type != 'credit_card' OR (closing_day BETWEEN 1 AND 31 AND due_day BETWEEN 1 AND 31))` (PostgreSQL aceita `!=`).
    - Comentário SQL curto referenciando edge cases de calendário (STATE.md) para Fase 5.
    Não alterar políticas RLS neste arquivo (colunas nullable para não quebrar linhas existentes).
  </action>
  <acceptance_criteria>
    - `rg -n "closing_day|due_day|accounts_credit_card_days_chk" supabase/migrations/20260423000000_accounts_credit_card_days.sql` retorna pelo menos 3 linhas de match.
    - `rg "CREATE TABLE public.accounts" supabase/migrations/20260423000000_accounts_credit_card_days.sql` não retorna linhas (apenas ALTER, não recriar tabela).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "closing_day|due_day" supabase/migrations/20260423000000_accounts_credit_card_days.sql && ! rg -q "CREATE TABLE public.accounts" supabase/migrations/20260423000000_accounts_credit_card_days.sql</automated>
  </verify>
  <done>Arquivo de migration versionado contém ALTER accounts + CHECK accounts_credit_card_days_chk.</done>
</task>

<task type="auto">
  <name>[BLOCKING] Aplicar migrations: supabase db push</name>
  <files>supabase/migrations/20260423000000_accounts_credit_card_days.sql</files>
  <read_first>
    - supabase/config.toml
    - .env.local (se existir; senão .env.example para variáveis de URL/keys)
  </read_first>
  <action>
    Com o Docker do Supabase local em execução (`supabase status` OK), executar no repositório: `supabase db push` (TTY). Em ambiente **non-TTY** (CI/agente sem login), documentar no SUMMARY da fase o comando exato `supabase db push` e o erro se falhar por token; não contornar com `--password` em texto plano no repo.
    Se o push falhar apenas por projeto não linkado, pausar e sinalizar no SUMMARY: usuário deve rodar `supabase link` ou push local após `supabase start`.
  </action>
  <acceptance_criteria>
    - Última linha de saída do comando contém `Finished supabase db push` OU `Applied migration` (string presente em stdout).
    - Se o comando não puder ser executado (sem daemon), o executor define `autonomous: false` equivalente reportando bloqueio no SUMMARY — neste plano `autonomous` já é `false` no frontmatter.
  </acceptance_criteria>
  <verify>
    <automated>supabase db push 2>&1 | tee /tmp/supabase-db-push.log; rg -n "Finished supabase db push|Applied migration|No new migrations" /tmp/supabase-db-push.log</automated>
  </verify>
  <done>CLI reporta push aplicado ou sem novas migrations; log salvo em /tmp/supabase-db-push.log.</done>
</task>

<task type="auto">
  <name>Regenerar types/database.ts para accounts</name>
  <files>types/database.ts</files>
  <read_first>
    - types/database.ts
    - package.json (scripts; ausência de script gen é aceitável)
  </read_first>
  <action>
    Atualizar manualmente `types/database.ts` nas interfaces `accounts.Row`, `accounts.Insert`, `accounts.Update` incluindo `closing_day: number | null` e `due_day: number | null` (Insert/Update opcionais `?`). Manter union de `type` inalterada (`savings` permanece só no tipo TS).
    Preferência: se `supabase gen types typescript --local` estiver disponível e passar, pode sobrescrever o arquivo gerado desde que preserve convenções existentes do projeto.
  </action>
  <acceptance_criteria>
    - `rg -n "closing_day" types/database.ts` encontra ocorrências em Row, Insert e Update de `accounts`.
    - `npm run lint` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "closing_day" types/database.ts && npm run lint</automated>
  </verify>
  <done>Tipos `accounts` incluem closing_day e due_day; lint passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Postgres | DDL executado com privilégios de migration; não expõe dados ao cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `accounts` CHECK | mitigate | Constraint `accounts_credit_card_days_chk` limita dias 1–31 apenas quando `type = 'credit_card'` |
| T-02-02 | I | N/A schema-only | accept | Migration não altera superfície de rede |
</threat_model>

<verification>
Após as três tarefas: `npm run lint` e inspeção Studio opcional (`closing_day`/`due_day` visíveis na tabela `accounts`).
</verification>

<success_criteria>
- Migration versionada commitada com CHECK de dias do cartão.
- `supabase db push` aplicou a migration no ambiente onde o comando rodou.
- Tipos TS compilam e refletem as novas colunas.
</success_criteria>

<must_haves>
- Cobertura D-09/D-10: dias coletados no fluxo de criação de cartão será implementado no plano 02-03; schema pronto aqui (ACC-03).
</must_haves>

<output>
Após conclusão: `.planning/phases/02-account-management/02-01-SUMMARY.md`
</output>
