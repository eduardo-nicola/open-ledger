---
phase: 02-account-management
plan: 2
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/accounts/schema.ts
  - lib/accounts/actions.ts
  - lib/accounts/queries.ts
  - lib/accounts/balance-history.ts
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
  - ACC-05
must_haves:
  truths:
    - "Server Actions criam/atualizam/arquivam contas com Zod e sessão Supabase server"
    - "Saldo consolidado soma apenas checking e digital_wallet não arquivadas (exclui credit_card e savings)"
    - "Função de série temporal ACC-05 documentada como constante até Fase 3"
  artifacts:
    - path: lib/accounts/actions.ts
      provides: "CRUD servidor + revalidatePath"
      contains: "'use server'"
    - path: lib/accounts/balance-history.ts
      provides: "Regra ACC-05 pré-transações"
      contains: "pré-Fase 3"
  key_links:
    - from: "lib/accounts/actions.ts"
      to: "/accounts"
      via: "revalidatePath('/accounts') e revalidatePath('/accounts/[id]')"
      pattern: "revalidatePath"
---

<objective>
Implementar camada de domínio **contas**: validação Zod alinhada a D-01/D-02/D-11, Server Actions para criar/editar/arquivar (e desarquivar se aplicável), query de **saldo consolidado** (ACC-04, D-07) e módulo **único** da fonte do histórico ACC-05 antes da Fase 3.

Propósito: concentrar regras e segurança no servidor antes das páginas.

Saída: `lib/accounts/*.ts` com contratos estáveis consumidos pelo plano 02-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@lib/supabase/server.ts
@types/database.ts
@.planning/phases/02-account-management/02-CONTEXT.md
@.planning/phases/02-account-management/02-UI-SPEC.md
</context>

<interfaces>
Do `types/database.ts` (após plano 02-01), usar `Database['public']['Tables']['accounts']['Row']` e campos `closing_day`, `due_day`, `archived_at`, `type`, `balance`.
</interfaces>

<tasks>

<task type="auto">
  <name>Zod schema e paleta (ACC-01, D-11, D-02)</name>
  <files>lib/accounts/schema.ts</files>
  <read_first>
    - types/database.ts
    - .planning/phases/02-account-management/02-CONTEXT.md (D-01, D-02, D-11)
    - .planning/phases/02-account-management/02-UI-SPEC.md (Paleta de cor)
  </read_first>
  <action>
    Criar `lib/accounts/schema.ts` exportando:
    - Enum Zod `accountUiType` com literais exatamente `checking`, `digital_wallet`, `credit_card` (nunca `savings` — D-02).
    - `accountNameSchema`: string `.min(1).max(120)`.
    - `accountColorSchema`: union de **10** strings literais estáveis (ex.: tokens `emerald`, `sky`, …) mapeáveis a classes Tailwind ou hex fixos no UI — documentar mapa exportado `ACCOUNT_COLOR_SWATCHES` constante array length 10.
    - `creditCardDaysSchema`: objeto com `closing_day` e `due_day` `z.number().int().min(1).max(31)`; aplicar `.superRefine` ou discriminated flow: obrigatório somente quando tipo efetivo for `credit_card` (D-09).
    - `createAccountSchema` / `updateAccountSchema` combinando campos; `updateAccountSchema` permite `archived_at` opcional para arquivar (ISO string ou null para desarquivar).
  </action>
  <acceptance_criteria>
    - `rg -n "z.enum" lib/accounts/schema.ts` não contém a substring `savings` na mesma linha que `z.enum` (enum Zod da UI só com os três tipos — D-02).
    - `rg -n "ACCOUNT_COLOR_SWATCHES|accountUiType" lib/accounts/schema.ts` retorna matches.
    - `npm run lint` exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "z.enum.*savings" lib/accounts/schema.ts && rg -n "ACCOUNT_COLOR_SWATCHES" lib/accounts/schema.ts && npm run lint</automated>
  </verify>
  <done>schema.ts criado com enums/paletas sem savings na UI Zod.</done>
</task>

<task type="auto" tdd="false">
  <name>Server Actions + queries consolidadas (ACC-01, ACC-02, ACC-03, ACC-04)</name>
  <files>lib/accounts/actions.ts, lib/accounts/queries.ts</files>
  <read_first>
    - lib/supabase/server.ts
    - lib/accounts/schema.ts
    - .planning/phases/02-account-management/02-RESEARCH.md (Pattern Server Function + revalidatePath)
  </read_first>
  <action>
    Criar `lib/accounts/actions.ts` com `'use server'` no topo do arquivo. Funções exportadas (nomes exatos):
    - `createAccount(input: unknown)` — parse `createAccountSchema`, `createClient` de `@/lib/supabase/server`, INSERT em `accounts` com `user_id` = `user.id` da sessão (`auth.getUser()`), para `credit_card` persistir `closing_day`/`due_day`; para demais tipos enviar `closing_day: null`, `due_day: null`. Após sucesso: `revalidatePath('/accounts')`.
    - `updateAccount(input: unknown)` — parse `updateAccountSchema`, UPDATE por `id` com `.eq('user_id', user.id)` implícito via RLS + filtro `id`, revalidar `/accounts` e `/accounts/[id]` (usar template literal com id dinâmico: `revalidatePath(\`/accounts/${id}\`)` e `revalidatePath('/accounts')`).
    - `archiveAccount(formData: FormData)` e `unarchiveAccount(formData: FormData)` — receber `id` do FormData; arquivar = `archived_at: new Date().toISOString()`, desarquivar = `archived_at: null`. Revalidar mesmas rotas.
    Usar `z.safeParse`; em falha retornar objeto `{ error: string }` serializável (não lançar stack cru para UI).
    Criar `lib/accounts/queries.ts` **sem** `'use server'` exportando:
    - `getAccountsForUser()` — SELECT contas ordenadas por `created_at` desc.
    - `getConsolidatedBalanceCents()` — soma `balance` onde `archived_at is null` AND `type IN ('checking','digital_wallet')` **excluindo explicitamente** `savings` e `credit_card` (alinha à tabela do 02-UI-SPEC.md).
  </action>
  <acceptance_criteria>
    - `rg -n "export async function createAccount|export async function updateAccount|archiveAccount|unarchiveAccount" lib/accounts/actions.ts` encontra as quatro assinaturas.
    - `rg -n "getConsolidatedBalanceCents" lib/accounts/queries.ts` e `rg -n "digital_wallet" lib/accounts/queries.ts` com `rg -n "checking" lib/accounts/queries.ts` no mesmo arquivo.
    - `rg -n "revalidatePath" lib/accounts/actions.ts` com pelo menos 2 ocorrências.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "export async function createAccount" lib/accounts/actions.ts && rg -n "getConsolidatedBalanceCents" lib/accounts/queries.ts && npm run lint</automated>
  </verify>
  <done>actions.ts e queries.ts exportam funções nomeadas com revalidatePath e soma consolidada.</done>
</task>

<task type="auto">
  <name>Fonte ACC-05: série constante até Fase 3</name>
  <files>lib/accounts/balance-history.ts</files>
  <read_first>
    - .planning/phases/02-account-management/02-RESEARCH.md (Open Questions ACC-05)
    - supabase/migrations/20260416000000_initial_schema.sql (coluna balance)
  </read_first>
  <action>
    Criar `lib/accounts/balance-history.ts` exportando função assíncrona `getBalanceHistoryForAccount(accountId: string, days: number)` (default `days = 30`) que:
    1. Usa `createClient` server e lê uma linha `accounts` por `id` (RLS).
    2. Retorna array de `{ date: string; balanceCents: number }` com **exatamente `days` elementos** — datas consecutivas encerrando em **hoje (UTC date do servidor)** para simplicidade, documentado em comentário JSDoc.
    3. Para **cada** ponto, `balanceCents` = `row.balance` (valor idêntico — série horizontal). Comentário de bloco no arquivo contendo a frase exata: `ACC-05 pré-Fase 3: série constante = accounts.balance em cada dia; Fase 3 substituirá por acumulado de transações pagas.`
    Não consultar `transactions` nesta fase para o gráfico (evita divergência com saldo ainda não sincronizado por TXN-01).
  </action>
  <acceptance_criteria>
    - `rg -n "ACC-05 pré-Fase 3: série constante = accounts.balance" lib/accounts/balance-history.ts` retorna exatamente 1 linha.
    - `rg -n "getBalanceHistoryForAccount" lib/accounts/balance-history.ts` retorna match.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "ACC-05 pré-Fase 3: série constante = accounts.balance" lib/accounts/balance-history.ts && rg -n "getBalanceHistoryForAccount" lib/accounts/balance-history.ts</automated>
  </verify>
  <done>balance-history.ts documenta série constante e exporta getBalanceHistoryForAccount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente → Server Action | payloads não confiáveis |
| Server → Supabase | JWT do usuário; RLS no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T | `createAccount` / `updateAccount` | mitigate | `z.safeParse` + CHECK no DB + colunas cartão só quando `type=credit_card` |
| T-02-04 | E | `getBalanceHistoryForAccount` | mitigate | SELECT só por `id`; RLS restringe `user_id` |
| T-02-05 | I | logs | accept | Não logar PII além do necessário |
</threat_model>

<verification>
`npm run lint` após criar os quatro arquivos; revisão estática de que nenhuma action aceita `user_id` do cliente (derivar sempre da sessão).
</verification>

<success_criteria>
- CRUD servidor cobre ACC-01/02/03 com revalidação pós-mutação (D-05).
- Consolidado implementa exclusão de cartão e arquivadas (D-07) e não soma savings (UI-SPEC).
- Regra ACC-05 explícita e grepável no código.
</success_criteria>

<must_haves>
- Matriz decisão: D-01..D-11 endereçadas neste plano ou no 02-03; D-05 via `revalidatePath`.
</must_haves>

<output>
Após conclusão: `.planning/phases/02-account-management/02-02-SUMMARY.md`
</output>
